> 상태: **승인 대기**

# 1-되먹임 — 고유 컨텍스트

**이 계획은 코드를 거의 안 고친다.** 하는 일은 **판정**이다 — 지금 되먹임이 몇 개이고,
어느 것을 어떻게 없앨 수 있나. 2·3 이 *"고리를 안 늘렸다"* 를 증명하려면 이 수가 먼저다.

## 반드시 읽을 것

- `modulePacking.ts:431`(1차) · `:446-482`(사다리) · `:487`(tidy-tree) · `:572`(lineEnds) ·
  `:597`(2차) — **다섯 자리가 이 계획의 전부다.**
- `module/moduleTransform.ts:230` `moduleExtent` — 높이가 무엇으로 정해지나

## 하지 않는 것

- **사다리를 없애는 것.** 이 계획은 *"없앨 수 있나, 조건이 무엇인가"* 까지다.
  실제 제거는 `3-순서` 가 못을 줄인 뒤에야 판단할 수 있다.
