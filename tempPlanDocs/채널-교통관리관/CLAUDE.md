# 채널-교통관리관 — 상태와 고유 컨텍스트

> 상태: **승인 대기** (2026-09-07)
>
> **한 문장:** 열 채널에는 교통관리관이 있고 행 채널에는 없다. 그 하나를 전치해서 세운다.

## 왜 이 계획인가 — **없는 것을 만드는 게 아니라 있는 것을 옮긴다**

`channelGeometryPlanner` 가 이미 교통관리관이다. 스스로를 *"통합 장부"* 라 부르고,
납품과 반출을 **한 장부에서 같이** 배정하고, 신고를 **구간이 아니라 도형**(`Shape{h,v}`)으로
받고, 트랙 수가 그 결과로 나온다. 행 채널에는 그게 없다 — 가로 구간 하나만 받는다.

```
             열 채널 (오늘 있는 것)              행 채널 (이 계획이 세울 것)
프레임        (추상 트랙 **열**, 절대 **행**)      (로컬 x **열**, 추상 트랙 **행**)
신고          Shape { h[], v[] }                 Shape { h[], v[] }
관리 대상      납품 계단꼴 + 반출 elbow            납품 가로선+**다리** + **self 반출** 세로선
산출          trackCount → **폭**                 trackCount → **높이**
변환          트랙 → 절대 x                       트랙 → 절대 y
```

## 이 폴더의 어휘

```
교통관리관     한 영역의 **모든 종류**의 경로를 **한 장부**에서 도형으로 배정하는 것
다리          포트/상자에서 자기 트랙 행까지 내려오(올라오)는 **세로** 주행
self 반출     상자에서 바깥 N/S 변까지 **자기 x 로 직진**하는 반출. 오늘 어느 장부에도 없다
로컬 x        모듈 extent 왼쪽 변 기준 x. 같은 깊이 안에서 절대 x 의 **상수 평행이동**이다
```

## 착수 전 반드시 읽을 것

| 무엇 | 왜 |
|---|---|
| `planner/channelGeometryPlanner.ts` 머리말 + `Shape`(:176) + `shapesConflict`(:194) | **베낄 원본.** 이 계획은 이 파일의 전치를 만든다 |
| `planner/modulePacking.ts` :1106–1154 | 열 채널이 **1패스**라는 증거 — 도형 먼저, 폭은 `trackCount` 에서 |
| `planner/rowChannelPlanner.ts` — `RowCrossing`(:37) 과 전순서 머리말 | 오늘 신고가 `{id, side, x1, x2}` 뿐이라는 증거. **전순서는 도형 장부의 근사다** |
| `planner/modulePacking.ts` :843–844 (`x1: 0` / `x2`) | 신고가 만들어지는 유일한 자리 |
| `planner/deliveryRoute.ts` :650 (`if (g.fromRowChannel) push(s)`) | 다리를 그리는 **유일한 한 줄**. 계획엔 자료가 없다 |
| `planner/perimeterTrackPlanner.ts` :39 (`kind: "self"`) + `modulePacking.ts` :1330 | self 반출이 **마진 행 채널을 세로로 관통**하는데 사후 기록만 된다 |
| `docs/auto-layout/common/pipeline-lifecycle.md` | 시대·장부표. **§5 규칙 ②는 틀렸다 — Step 0 이 고친다** |
| `tempPlanDocs/구간-밖-주행/` | **이 계획이 그 B 트랙(B1·B2·B3)을 흡수한다.** A 트랙(기둥 밖 장부)은 그쪽에 남는다 |
| `tempPlanDocs/채널-양보-명세/judgements.md` J-교차 | ⓐ(전순서)가 이 계획으로 **불필요해진다**. ⓑⓒ 는 여전히 미결 |

## 하지 않는 것

- **열 채널을 안 건드린다.** 이미 정답 모양이다. 베끼되 고치지 않는다.
- **행 채널에 dogleg·지하를 안 넣는다.** 이 계획은 **⑴ 발견**(도형을 장부에 올린다)까지다.
  **⑵ 해결**(겹치면 다른 도형을 고른다)은 J-교차 ⓑⓒ 소관이다. 그래서 이 계획이 끝나면
  **오늘 조용하던 것이 시끄러워진다** — 그게 성공이지 실패가 아니다.
- **모듈 몸통을 뚫는 다리는 범위 밖이다.** gap 포트의 다리가 자기 모듈 몸통을 지나는 건
  *"그 포트가 애초에 못 나온다"* 는 **모듈 층** 문제다. 사유만 갈라 보여 준다.
- **기둥 밖 장부(`outside`)의 A2 를 앞당기지 않는다.** 그건 `구간-밖-주행` 에 남는다.
- **왼쪽 정렬 항등식을 안 건드린다** (사장님 지시로 보류, 2026-09-07).

## 공통 전제 (썩는지 감시할 것)

| 전제 | 확인 방법 | 확인일 |
|---|---|---|
| 열 채널은 **1패스**다 — 도형 배정이 폭보다 **앞**이고, 뒤의 호출은 좌표 **번역**뿐 | `modulePacking` :1133 → :1148 → :1154 → :1215 | 2026-09-07 ✔ |
| 행 채널 신고는 `{id, side, x1, x2}` 뿐 — **세로가 없다** | `rowChannelPlanner.RowCrossing` | 2026-09-07 ✔ |
| 다리를 그리는 코드가 **한 줄**이고 계획엔 자료가 없다 | `deliveryRoute.buildPlannedChain` :650 | 2026-09-07 ✔ |
| self 반출은 상자의 **자기 x** 로 바깥 변까지 세로 직진한다 | `modulePacking` :1330 `addSeg(anchor, {x: anchor.x, y: seatRow})` | 2026-09-07 ✔ |
| 채널을 타는 반출은 **이미** 열 채널 통합 장부의 신고자다 | `modulePacking` :1121–1128 `exps.push(...)` | 2026-09-07 ✔ |
| `절대 x = colX[깊이] + 로컬 x` — 한 행 채널은 **한 깊이**뿐이라 상수 평행이동 | `modulePacking` :1163 `shiftModule(m, colX[d] - ext.x, …)` | 2026-09-07 ✔ |
| `planTracks` 는 `topY` 를 받는다 → 행 채널 **뒤**에 선다 (Step 3 의 장애물) | `modulePacking` :1096 | 2026-09-07 ✔ |
| 기준선 = 타입 0 · **기존 실패 2건**(trunkPipe 유체 면) | `npx tsc -p tsconfig.app.json --noEmit` · `npx vitest run` | 2026-09-06 ✔ |

## 실제로 밟은 함정

(착수 후 채운다.)
