---
tags: [planning, auto-layout, routing]
---

# 벨트-레인 — 한 줄을 22.5 + 22.5 로 가른다

**한 줄 요약:** 벨트 한 줄이 실제로 가진 **좌/우 두 레인**을 모델의 낱말로 들인다.
45/s 벨트 한 줄이 **두 품목을 22.5씩** 나르게 되면, 저율 품목이 줄·포트·채널 트랙·면
깊이를 통째로 먹던 낭비가 절반이 된다.

---

## 1. 푸는 문제 — 오늘은 한 줄에 한 품목뿐이다

`Link` 하나 = 물리 벨트 하나 = **품목 하나**다(`module/link.ts`). 그래서
`iron-gear-wheel` 2/s 짜리 줄도 45/s 벨트 한 줄을 통째로 쓰고, 그 줄이 먹는 것은
벨트 칸만이 아니다:

```
포트 한 쌍          모듈 양쪽에 인서터+상자 자리
채널 트랙 하나       모듈 사이 통로의 세로줄 하나
면 깊이 하나         머신 면 바깥 한 칸 (다른 줄이 그 깊이를 못 쓴다)
납품 경로 하나       채널을 가로지르는 belt 체인
```

**이용률이 5% 여도 값은 100% 다.** 이게 `known-limits` §10(면당 깊이가 모자라 줄이
못 앉는다)과 채널 폭 부풀림의 공통 원인 한쪽이다.

### 레인은 `split belt` 와 **직교한다** — 헷갈리면 안 된다

용어사전이 배선 형태를 **두 축**으로 읽는다(§배선 형태 셋). 레인은 그 두 축 어디에도
없는 **세 번째 축**이다:

| 축 | 묻는 것 | 답 |
|---|---|---|
| 벨트 → 흐름 | 한 줄이 흐름을 몇 개 싣나 | 하나 = 다이렉트 · 여럿 = 트렁크 |
| 흐름 → 벨트 | 한 흐름이 줄 몇 개에 실리나 | 여럿 = **split belt** |
| **줄 → 레인** | **한 줄이 품목을 몇 개 싣나** | **둘 = 레인 공유(MixedItemBelt)** |

`split belt` 는 **양이 넘쳐서 쪼개는 것**(강제), 레인 공유는 **양이 남아서 합치는
것**(선택)이다. 방향이 반대다.

---

## 2. 물리 — 레인 규칙 일곱 (2026-09-03 사장님 확정)

**이 표가 이 계획서의 나머지 전부를 떠받친다.** 저장소 문서에는 ①②만 있었고,
③~⑦ 은 **어디에도 없었다** — 아래 §2.1 이 그 확인이다.

| # | 규칙 | 출처 |
|---|---|---|
| ① | **인서터는 언제나 「먼 레인」에 떨군다.** 벨트에 **직교해** 서든 **나란히** 서든 같다 | 용어사전 §MixedItemBelt(직교) · 사장님(나란함, 2026-09-03) |
| ② | **인서터는 두 레인 다 집는다** → 품목을 고르려면 **필터**가 필요하다 | 용어사전 §MixedItemBelt |
| ③ | **사이드로드는 레인을 접는다** — 옆에서 합류하는 벨트의 **두 레인 전부**가, 대상 벨트에서 **자기가 닿은 쪽 레인 하나**로 들어간다 | 사장님 2026-09-03 |
| ④ | **지하벨트는 레인을 보존한다**(입구 → 출구) | 사장님 2026-09-03 |
| ⑤ | **직진 투입(뒤에서 들어옴)은 두 레인을 1:1 보존한다** | 문서엔 *"직진 투입"* 이라는 **이름만** 있다(용어사전 §흐름-인접 · belt-flow-inspection §3). 레인 보존은 **직선 벨트가 이어지는 것과 같은 사실**이라 따로 안 물었다 |
| ⑥ | **곡선은 별개의 규칙이 아니라 「유입이 하나뿐인 칸」의 이름이다.** 유입이 둘 이상이면 그 칸은 직진 + 사이드로드가 되고 ③이 걸린다 | 사장님 2026-09-03 |
| ⑦ | **옆에서 올라타려면 대상 레인에 여유가 있어야 한다** — 그 레인이 이미 최대면 못 올라탄다 | 사장님 2026-09-03 |

### ③ 의 실물 — 양쪽에서 하나로

```
        A →→→→→ ┐ ┌ ←←←←← B          A(서→동) 는 C 의 **서쪽** 레인으로
                 [C]                   B(동→서) 는 C 의 **동쪽** 레인으로
                  ↓  (또는 ↑)          A·B 각자의 두 레인은 그 한 레인으로 **접힌다**
```

이 규칙에서 곧바로 나오는 두 결론:

1. **합류를 지나는 지류의 상한은 레인 하나(22.5/s)다.** 상류에서 45/s 로 가득 찬
   벨트여도 합류 칸을 통과하는 순간 절반만 흐른다. → §7 Step 4 의 자격 조건
   *"각자 rate ≤ 레인 용량"* 은 **모델링 선택이 아니라 물리**다.
2. **레인의 신원이 합류 칸 하나에서 국소적으로 정해진다.** ④(지하 보존)와 함께
   보면, 한번 정해진 레인은 그 물리 줄 끝까지 간다 — **레인 장부의 낟알이 「물리 줄
   하나」로 닫힌다.** 전역 추적이 필요 없다. 이게 §5 가 「양쪽 합류」를 고르는 이유다.

### ⑥ 의 실물 — **곡선은 유입이 하나일 때만이다**

벨트가 90° 꺾이면 좌/우 레인은 **진행 방향 기준으로 유지된다**(동쪽으로 가던 왼쪽 =
북쪽 절반이, 북쪽으로 꺾인 뒤에도 왼쪽 = 서쪽 절반). 우리 벨트는 늘 꺾이므로
(`emitInputLinks` 의 끝 칸 꺾기 · 납품 경로의 모서리) **이게 참이라야 `side` 가 필드
하나로 산다.** 참이다 — 단, **그 칸의 유입이 하나일 때만.**

유입이 둘이 되는 순간 그 칸은 곡선이 아니라 **직진 벨트 + 사이드로드**다. 누가 접히는지는
**그 칸 자신의 방향**이 정한다:

```
(나-1)  K 가 북쪽으로 간다                (나-2)  K 가 동쪽으로 간다

             ↑ 나감                                    ↑
   A →→→→→ [ K ]                          A →→→→→ [ K ] →→→→→ 나감
             ↑                                        ↑
             B                                        B

   B = 뒤(남)에서 → 직진 → 두 레인 유지      A = 뒤(서)에서 → 직진 → 두 레인 유지
   A = 옆(서)에서 → 접힘 → 왼쪽(서) 레인     B = 옆(남)에서 → 접힘 → 오른쪽(남) 레인
   → **A 가 곡선을 잃는다**                  → **B 가 보존되지 않는다**
```

**(나-2)에는 애초에 꺾임이 없다** — A 는 직진하고 B 가 옆에서 붙는 평범한 사이드로드다.
같은 그림이 K 의 방향 하나로 갈린다.

| 그 칸의 유입이 | 결과 |
|---|---|
| **하나뿐 + 옆에서** | **곡선** — 좌/우 보존 |
| 하나뿐 + 뒤에서 | 직진 — 좌/우 보존 |
| **둘 이상** | 곡선이 아니다. **뒤에서 온 것만** 두 레인 유지, **옆에서 온 것은 전부 접힘** |

*"뒤"* = 그 칸 자신의 방향의 **반대편**. 유입이 둘 다 옆이면(§5 의 합류 칸) 직진이
없으므로 **둘 다 접힌다** — 그게 ③의 그림이다.

### ⑦ 이 합류를 **비싼 연산으로 만든다**

옆에서 오는 쪽은 대상 레인에 **틈이 있을 때만** 올라탄다. 그래서 (나-1)이면 A 가 B 의
왼쪽 레인 틈을, (나-2)면 B 가 A 의 오른쪽 레인 틈을 노려야 하고, 뒤에서 온 쪽이 그
레인을 꽉 채우고 있으면 **옆 쪽은 조용히 굶는다.**

> **합류는 여유가 있을 때만 쓴다**(사장님 2026-09-03). 이 계획이 합류를 **한 자리에만**
> 두고 자격 조건을 넷이나 거는 이유가 이것이다 — 벨트는 이어져 있고 총량도 맞는데
> **한쪽만 안 흐르는** 실패 모양이라, 사후에 눈으로 못 잡는다.

**§5 의 3칸 합류가 이 함정을 구조로 피한다** — 합류 칸에 **뒤 유입이 아예 없어서**
아무도 레인을 선점하지 않는다. 두 지류가 각자 빈 레인에 올라탄다.

### §2.1 — 저장소 문서에 ③이 없다는 확인 (2026-09-03)

| 자리 | 적혀 있는 것 | 레인 |
|---|---|---|
| `용어사전.md` §side-load | *"벨트 옆면으로 아이템이 합류하는 것. 흐름-인접의 한 형태"* | ✗ |
| `용어사전.md` §흐름-인접 | *"서로 다른 라우팅에서는 **항상 의도치 않은 오염**"* | ✗ |
| `entity-roles.md` §C | *"라인 합류는 splitter 또는 측면 합류로만"* | ✗ |
| `용어사전.md` §MixedItemBelt | *"사이드로드 없이는 **불가능**"* — 답이라는 것만, **어떻게**는 없음 | ✗ |
| `UI/belt-flow-inspection.md` §3 | *"유입 = 뒤/옆(side-load). 정면 마주보기 유입 불가"* | ✗ |

저장소는 사이드로드를 **"오염이 일어나는 기하"** 로만 알고 있었다. 그래서
`analysis/beltFlow.ts` 가 *"한 벨트 = 1품목"* 으로 근사할 수 있었다.

---

## 3. 오늘 코드에서 레인은 어디에 있나 — **수량은 이미 다 있다**

```
beltThroughput.ts:7   itemsPerSec = belt_speed × 480
                      ( = belt_speed × **2 레인** × 4 아이템/타일 × 60 틱 )
```

express 0.09375 → **45**. 프로토타입 문서(`TransportBeltConnectablePrototype::speed`)의
`speed × 480 = items/second` 와 같은 상수다. 즉 **오늘의 모든 용량 수치가 이미 두 레인
합**이고, 레인 하나는 **정확히 절반**이다. 새 게임데이터도 새 측정도 필요 없다.

①이 확정됐으므로 `inserter_drop_position` 은 **레인을 고르는 데 안 쓴다** — 언제나 먼
레인이라 물어볼 것이 없다(`analysis/beltFlow.ts` 가 드랍 **칸**을 구하는 데만 쓴다).
그 값이 다시 필요해지는 것은 Step 6(가까운 레인 드랍)뿐이다.

그 수를 보는 자리는 셋뿐이다:

| 자리 | 오늘 무엇을 보나 | 레인이 들어오면 |
|---|---|---|
| `determineBeltCount(rate, belts)` | **줄** 용량 — "이 수요에 몇 줄" | **안 바뀐다.** 줄 수는 여전히 줄 용량이 정한다 |
| `createLinks` 의 `cap: tier.throughput` | **줄** 용량 — 붓기의 상한 | 공유 줄에 부을 땐 **레인 용량**이 상한 |
| `analysis/beltFlow.ts` 의 `capacity` | **줄** 용량 — 포화 판정 | 레인별로 나뉘어야 "혼합" 표시가 참이 된다 |

---

## 4. 왜 **공급(supply) 쪽만** 가능한가 — 기하가 낸 결론

용어사전은 이 비대칭을 *"출력은 사이드로드 없이는 불가능"* 이라고만 적었다. ①이
확정됐으므로 이제 **왜 그런지가 기둥 모델에서 증명된다:**

```
① 드랍은 먼 레인이다 (나란해도 그렇다)   →  같은 쪽에 선 두 팔은 **같은 레인**에 떨군다
② 모듈 면 벨트는 한쪽에만 머신이 있다     →  머신 팔은 전부 **같은 쪽**에서 떨군다
──────────────────────────────────────────────────────────────
∴ 인서터만으로는 모듈 안에서 두 레인을 **채울 수 없다**
```

②는 `emitInputLinks` 의 `faceCell(geomExt, face, d, t)` 가 말한다 — 벨트는 깊이 `d`,
좌석은 언제나 `d1`(머신 쪽). 벨트의 반대쪽(`d+1`)은 포트나 더 깊은 줄이지 머신이 아니다.
①의 *"나란해도 먼 레인"* 이 마지막 구멍을 막는다 — 관통 포트(`portEnd = N/S`)의 포트
인서터가 벨트와 나란히 서는데, 그것도 먼 레인이므로 **예외가 없다.**

**그래서 레인은 밖에서 갈려서 들어와야 한다.** 다행히 경계가 이미 뚫려 있다:

```
deliveryRoute.stripKeys  →  링크 포트의 경계 인서터를 **뗀다** (belt→belt)
```

즉 납품 경로 벨트가 모듈 면 벨트로 **끊김 없이 이어진다.** 레인 정보가 경계에서 안
죽는다. 경계에 인서터가 남은 포트(1:1 다이렉트)는 그 순간 ①이 다시 걸려 두 품목이 한
레인에 겹친다 — **레인 공유는 링크 포트 전용**이라는 조건이 여기서 나온다.

**공급 쪽에서 필요한 나머지 하나는 필터다**(②). 탭 인서터가 자기 품목만 집어야 한다.

> **이 결론은 Step 6 이 뒤집는다.** 가까운 레인에 떨굴 수단이 생기면 같은 쪽 두 팔이
> 두 레인을 채우고, 그러면 모듈 **안에서** 레인을 만들 수 있고 **출력 레인까지** 열린다.
> 그래서 Step 6 은 편의 기능이 아니라 **기하의 갈래를 고르는 스위치**다.

---

## 5. 어디서 섞나 — **양쪽 합류 3칸**으로 확정

| | 안 | 판정 |
|---|---|---|
| **(i)** | **양쪽 합류** — 합류 칸 `C` 로 두 지류가 **마주 보는 두 옆면**에서 들어온다 | **채택** |
| (ii) | 한쪽 합류 — 본류가 직진하고 지류 하나만 옆에서 붙는다 | **기각** — 아래 |
| (iii) | splitter 로 레인 밸런싱 | 기각 — `placement-search` §10.1 의 비-목표 |

**(ii) 를 기각하는 이유가 ③⑤⑦에서 바로 나온다.** 본류는 뒤에서 들어오므로 ⑤로 **두
레인을 유지**하고, 지류는 ⑦에 따라 **그 레인에 틈이 있을 때만** 올라탄다. 그런데 본류의
어느 레인이 얼마나 찼는지는 **본류의 상류 이력**에 달렸다(인서터로 실렸으면 먼 레인,
사이드로드로 실렸으면 그쪽 레인). 즉 **레인 장부가 전역이 된다.**

(i)은 두 지류가 **둘 다** 사이드로드다 — 합류 칸에 **뒤 유입이 없어서** 아무도 레인을
선점하지 않고, 상류가 무엇이든 결과가 같다. 레인이 그 칸에서 **국소적으로** 정해진다.

```
        A →→→→→ ┐ ┌ ←←←←← B
                 [C]              C 이후: A = 한 레인, B = 다른 레인
                  ↓               ④ 로 지하 점프를 지나도 유지된다
```

### 이 기하가 요구하는 자리 — **채널이 이미 내주고 있었다** (2026-09-03 조사)

> **원래 여기 *"한 행에서 폭 3"* 이라고 적었다. 틀렸다.** 합류를 **가로**로 그려서
> 나온 값이었는데, 채널 안에서 자연스러운 방향은 **세로**다. 실제 값은 **트랙 하나**이고,
> 그나마 **둘이 쓰던 트랙 둘을 하나로 줄인다.**

`channelGeometryPlanner` 의 납품 경로는 이미 **계단꼴**이다 — 자식 E벽의 `startY` 에서
가로로 트랙 `T` 까지, `T` 위를 **세로로** 달려 `endY` 로, 다시 가로로 부모 W벽까지.
그 세로 주행이 가로로 꺾이는 칸 `(T, endY)` 가 **오늘도 이미 곡선**이다(유입 하나·옆에서).

두 납품이 **같은 트랙 `T` 를 쓰고 같은 `endY` 로 끝나면** 그 칸의 유입이 둘이 된다:

```
        A  (startY_A < endY)          A 는 **북에서** 내려온다
             │
      ───────┼──────→ 부모 W벽        칸 (T, endY) 은 **동쪽**으로 나간다
             │                         → 뒤(서) 유입이 없다  ✔ 조건 ⑵
        B  (startY_B > endY)          B 는 **남에서** 올라온다
```

- 유입 둘 다 **옆**이므로 직진이 없다 → ⑥에 따라 **둘 다 접힌다**
- A(북) → 동진 벨트의 **왼쪽** 레인 · B(남) → **오른쪽** 레인
- 세로 주행 구간이 `[startY_A, endY]` 와 `[endY, startY_B]` 로 **겹치지 않는다** —
  같은 트랙을 나눠 쓰는 것이 아니라 **이어 쓴다**

**자격 조건이 하나 늘어난다:** 두 자식 포트가 **부모 포트 행을 사이에 두고** 갈라져
있어야 한다. 같은 쪽이면 세로 주행이 겹쳐, 위쪽이 아래쪽의 **뒤 유입**이 되고 ⑦로
아래쪽이 굶는다. 행은 계획 시점에 이미 알므로 **앉혀 보지 않고 판정된다.**

### 그래서 합류는 새 도형이 아니라 **도형 둘을 하나로 합치는 것**이다

두 계단꼴은 `(T, endY)` 뿐 아니라 **`endY` 행의 가로 진출 구간 전체**를 공유한다.
그게 곧 물리 벨트 하나다. 오늘 `shapesConflict` 는 칸을 하나라도 나눠 쓰면 충돌로
보므로(정확히 §5 가 말한 *"오늘은 오염으로 막는다"*), 고칠 자리는 **판정이 아니라 입력**이다:

```
오늘   DeliveryInput{ id, startY, endY }  ×2   →  트랙 2 · 가로 진출 2 · 부모 포트 2
합류   DeliveryInput{ id, startY, startY2, endY } ×1 → 트랙 1 · 가로 진출 1 · 부모 포트 1
```

**합류 쌍을 예외로 허가하는 것이 아니라 애초에 한 경로로 넣는다** — 그러면
`shapesConflict` 도 `buildPlannedChain` 도 그대로 두고, 도형과 방출만 한 갈래 는다.
`buildPlannedChain` 의 `push` 열이 두 갈래 진입 + 한 갈래 진출이 되면 끝이다.

### 합류 칸의 조건 셋

```
⑴ 지상 벨트다              지하 끝에 사이드로드가 되는지는 ③에 없다 — 묻지 않고 막는다
⑵ **뒤 유입이 없다**        ⑤·⑦: 뒤에서 온 쪽이 레인을 선점하면 옆 쪽이 조용히 굶는다
⑶ 유입은 그 두 지류뿐이다   셋째가 붙으면 ⑥으로 성질이 또 바뀐다
```

⑴은 답을 몰라도 되게 **조인 것**이지 불가능 판정이 아니다. ⑵⑶은 ⑥⑦에서 나온
**필수 조건**이다 — 어기면 벨트는 멀쩡히 이어진 채 한쪽만 안 흐른다.

**계단꼴 합류는 셋을 구조로 만족한다**(위 조사) — ⑴ 세로 주행은 늘 지상이고(빈 트랙을
골랐다), ⑵ 합류 칸의 뒤(서)에서 오는 것이 없고, ⑶ 도형이 **하나**라 `shapesConflict`
가 셋째를 못 들이민다. 조건을 검사할 자리가 아니라 **못 어기는 기하**다.

### 반대 방향 제약 — **레인이 실린 줄에는 아무것도 붙이지 않는다**

⑥ 때문에, 레인이 갈린 줄의 **어느 칸이든** 옆에서 벨트가 붙는 순간 성질이 바뀐다:

- 그 칸이 **모서리**였다면 곡선을 잃고 그 줄이 통째로 한 레인으로 접힌다
- 그 칸이 **직선**이었다면 붙은 쪽이 접혀 들어와 남의 레인을 오염시킨다

오늘은 `beltFlowConflictCell` 이 이런 인접을 **전부** 막고 있어서 안전하다. 위험은
Step 4 가 *"허가된 합류"* 사전을 여는 순간 생긴다 — **허가한 합류가 우연히 남의 줄
옆에 앉을 수 있다.** 그래서 사전은 *"이 두 지류가 이 칸에서"* 까지 좁게 적고,
그 밖의 모든 인접은 지금처럼 오염으로 남긴다.

---

## 6. 자료 — 두 Link 가 한 물리 줄을 공유한다

| | 안 | 값 | 판정 |
|---|---|---|---|
| **(가)** | `Link.item: string` → `items: string[]` | `item` 이 하나라는 전제가 `carries`·`armsAt`·`IoLine`·포트 상자 `content`·`pairDeliveryPorts` 에 **전부** 박혀 있다. 그 전부가 배열이 된다 | 기각 |
| **(나)** | **Link 는 둘 그대로**, 둘이 같은 물리 줄을 가리킨다 (`share?: { lineId, side }`) | 방출기가 `lineId` 가 같은 둘을 **한 번만** 깐다. 필드 하나 + 방출기 한 곳 | **채택** |

**(나)의 결정적 근거:** `Link.carries` 의 불변식은 *"실린 총량 ≤ 그 벨트의 처리량"*
이다. 레인 공유는 그 불변식을 **깨는 게 아니라 상수만 바꾼다** — `처리량` →
`처리량 ÷ 2`. Link 를 합치면(가) 불변식 자체를 줄당/레인당 둘로 갈라야 한다.

> **착수해 보니 필드가 하나면 됐다** (2026-09-03). 원래 `share = { lineId, side }` 로
> 적었는데, *"이 축을 계획이 답하나 방출이 답하나"* 를 던져 보니 **`side` 는 둘 다
> 아니었다** — 사이드로드가 접는 레인은 *"그 지류가 합류 칸의 어느 옆면에서 왔나"* 이고,
> 그건 **합류 기하**(세로 주행이 위에서 오나 아래서 오나)가 이미 알고 있다. 게다가
> **아무도 안 물어본다**: 집는 팔은 ②에 따라 **필터**로 품목을 고르지 레인을 고르지 않는다.
> 그래서 `Link.sharedLineId?: string` 하나로 줄였다 — 채우는 곳은 [shareLanes] 하나.

레인 이름 자체는 게임 API 를 그대로 쓴다(`LuaEntity::pickup_from_left_lane` /
`pickup_from_right_lane`) → **`LaneSide = "left" | "right"`**, 진행 방향 기준. 새 낱말을
만들지 않는다 — 다만 오늘 그 타입을 쓰는 곳은 아직 없다(기하가 생길 때 쓴다).

---

## 7. 단계

### Step 1 — 레인 물리를 **문서로 못 박는다** ✅ 완료 (2026-09-03)

> **산출:** [`docs/factorio/belt-lane-semantics.md`](../../docs/factorio/belt-lane-semantics.md)
> — 규칙 일곱 · 합류 세 그림 · 결론 셋 · *"우리 코드는 아직 레인을 모른다"* 표 ·
> 게임이 주는데 안 쓰는 손잡이 셋.
>
> **들어오는 간선 여섯을 함께 놓았다**(고아 문서 방지): `README` 표 ·
> 용어사전 §`레인 (lane)`·§`MixedItemBelt`·§`side-load`·§`흐름-인접` ·
> `entity-roles` §C · `pipe-semantics` 짝 문서 · `belt-flow-inspection` 한계 ·
> `known-limits` §2.
>
> **딸려 고친 것 둘:**
> - 용어사전 §MixedItemBelt 의 *"아이템 용량이 2배"* → **가짓수**가 2배이지 양이 아니다.
>   품목마다 쓸 수 있는 것은 **절반**(45 → 22.5 + 22.5).
> - `known-limits` §2 에 *"이 항목 아래 문제가 둘"* 을 적었다 — 같은 품목 합류와 레인
>   분리는 **다른 기전**이라, 처방 하나로 둘 다 안 풀린다.

- **전제:** §2 의 일곱이 **전부 확정됐다**(2026-09-03 사장님). 미확인 0.
- **불변:** 코드 변경 0. `git diff --stat src/` 가 비어 있다.
- **관문:** 물리 표가 닫혀야 Step 4 의 기하를 고를 수 있다. 그리고 이 표는 **계획이
  폐기돼도 남는다** — 게임 사실이라 우리 코드를 바꿔도 안 썩는다.

산출물: **`docs/factorio/belt-lane-semantics.md`** — `factorio/` 폴더에 둔다(게임
시맨틱스이지 우리 배치 결정이 아니다). `pipe-semantics.md` 와 같은 자리·같은 꼴
(*"벨트와 항목별 대조"* 처럼 **인서터 드랍 · 사이드로드 · 곡선 · 지하 · 여유**를 항목으로).

**들어갈 것:** §2 의 일곱 + §2.1 의 "문서에 없었다" 기록 + ③⑥의 실물 그림 +
거기서 나온 결론 셋:

```
지류의 상한은 레인 하나        ③ — 합류를 지나면 45/s 짜리도 절반만 흐른다
레인 신원은 국소적으로 정해진다  ③④⑥ — 합류 칸에서 정해지고 곡선·지하를 지나 유지된다
합류는 여유가 있을 때만         ⑦ — 대상 레인이 꽉 차 있으면 옆 쪽이 조용히 굶는다
```

용어사전 §`레인 (lane)` · §`MixedItemBelt` · §`side-load` · §`흐름-인접` 에서 이
문서로 링크를 건다. **`side-load` 항목이 특히 그렇다** — 오늘 거기 적힌 것은
*"오염의 한 형태"* 뿐이라, 레인을 배우려는 사람이 도달할 길이 없다.

### Step 2 — 낱말 되찾기: 코드의 "레인"을 **깊이**와 **트랙**으로 ✅ 완료 (2026-09-03, 범위 **A**)

용어사전이 이미 대체어를 갖고 있다 — 깊이는 `ClusterBeltDepth`, 반출 줄은 `트랙`.

- **관문:** *"깊이 3 레인의 오른쪽 레인"* 이라는 문장이 안 나오게 한다.
- **불변:** *기전* — 순수 식별자 치환이라 `tsc -p tsconfig.app.json` 과 615테스트가
  **전수 검사**한다. 문자열 리터럴과 문서 앵커(`#레인-lane`)만 손으로 확인한다.

> **착수해 보니 규모가 계획서의 추정보다 5배였다**(2026-09-03 실측). 원래 여기 적힌
> 수(`src 110 + 53 + 3 + 44 = 210`)는 **이름 몇 개만 grep 한 것**이라 나머지 서른 몇 개를
> 안 셌다. 실제:

```
식별자  541  (이름 40개)        레인(한글)  src 202 · docs 369
```

**두 뜻으로 갈린다:**

| 계열 | 뜻 | 자리 | 식별자 |
|---|---|---|---|
| **깊이** | 면 바깥 몇 칸 | `planner/module` 174 · `module` 41 · `execution` 57 · `debug` 43 | **≈ 315** |
| **트랙** | 반출·채널의 세로줄 | `perimeterLanePlanner` 88 · `modulePacking` 59 · `perimeterRouter` 29 · 나머지 45 | **≈ 221** |

**딸려 나온 사실 하나 — 깊이에는 이미 이름이 둘이다.** `PlannedLine.clusterBeltDepth`(10곳)와
`LinkFacePlan.laneDepth`(94곳)가 **같은 축·같은 단위**다. 그러니 깊이 계열 개명은 위생이
아니라 **오늘 있는 중복을 없애는 일**이고, 새 이름은 `beltDepth` 가 아니라 **이미 있고
용어사전과 같은 `clusterBeltDepth`** 여야 한다(같은 것에 이름 둘을 만들지 않는다).

**범위 셋 — 아직 안 골랐다:**

| | 범위 | 식별자 | 남는 위험 |
|---|---|---|---|
| **A** | 전면 | 541 + 산문 571 | 없음. 대신 diff 가 커서 리뷰가 안 된다 |
| **B** | **깊이 계열만** | ≈315 | 트랙 쪽 "lane" 이 남는다 — Step 4 가 채널을 건드리므로 거기서 다시 만난다 |
| **C** | 지금은 안 한다 | 0 | 코드에 새 "lane" 식별자를 **안 만들면**(§6 이 이미 `side` 로 정했다) 충돌이 **산문에만** 남는다. Step 3·4 를 먼저 하고 개명은 뒤로 |

> **직전 커밋이 바로 이 함정을 밟았다** — `d33c7e3 "이름 일괄 치환이 만든 잘못된 참조
> 넷을 고친다"`. 그래서 치환은 **한 계열씩, 타입검사를 통과시키며** 했다.

**실제로 한 것 (범위 A):**

| | 옛 | 새 |
|---|---|---|
| 축 | `laneDepth`(94) · `laneDepthsOf` · `laneDepthCap` | **`clusterBeltDepth`** — 이미 있던 `PlannedLine.clusterBeltDepth` 로 **통일**(중복 이름 제거) |
| 장부 | `claimLane` · `laneClear` · `laneCap` · `LaneShortage`(38) · `laneShortages` | `claimDepth` · `depthClear` · `depthCap` · `DepthShortage` · `depthShortages` |
| 예산 | `laneBudget.ts` · `LaneBudgetInput` · `TakenLanes` · `lanesOf` | `depthBudget.ts` · `DepthBudgetInput` · `TakenDepths` · `depthsOf` |
| 계측 | `faceLanes` · `deepLane` · `multiLaneFace` · `recordFaceLaneStats` | `faceDepths` · `deepBelt` · `multiDepthFace` · `recordFaceDepthStats` |
| 반출·채널 | `perimeterLanePlanner.ts` · `perimeter/lanes.ts` · `LaneAssignment` · `laneX` · `LaneOption`/`LanePlan`/`LaneHost`/`LaneContext`/`LanePortInput` · `channelLaneIntervals` · `planPerimeterLanes` | `perimeterTrackPlanner.ts` · `perimeter/tracks.ts` · `TrackAssignment` · `trackX` · `Track*` · `channelTrackIntervals` · `planPerimeterTracks` |
| 사유 문자열 | `"no free lane track in channel"` · `"no lane assignment"` | `"no free track in channel"` · `"no track assignment"` |
| 화면 | `flg.report()` 의 `면레인` · `깊은레인` | `면깊이` · `깊은줄` |

산문은 **깊이(축)** 와 **줄(그 깊이에 앉은 벨트)** 로 갈라 썼다 — `얕은 레인` → `얕은 줄`,
`레인 예산` → `깊이 예산`. 한글 조사도 함께 고쳤다(`깊이은`→`깊이는` 등 17파일).

**남긴 "레인" 은 전부 뜻이 맞는 자리다:**

```
beltThroughput.ts      "× 480 = 2 레인"           게임 레인 — 이 계획의 주제
deliveryRoute.ts       "half-lane side-load"      게임 레인
용어사전 §레인·§MixedItemBelt·§side-load           게임 레인
faceTable.ts · linkPlanner.ts  "옛 `lanes` 장부"   삭제된 장부의 이름(이력)
`lanes-exceed-capacity`                            삭제된 코드가 내던 문자열(이력)
```

**함정 하나 실제로 밟았다 —** `sed`/`grep` 의 `` 가 한글·`·` 같은 **멀티바이트 문자 앞에서
안 걸린다.** `laneX·(뒤집혔으면)` 이 조용히 안 바뀌었고, **`` 없는 평문 grep 으로 다시 세서야**
드러났다. 이후 전부 파이썬 `(?<![A-Za-z0-9_])…(?![A-Za-z0-9_])` 로 바꿨다.
**개명 검증은 `` 로 하면 안 된다.**

**계획 폴더들도 함께 고쳤다** — `자리-배정`·`계획-배정-개념정리`·`셀장부`·`부분-링크`·
`행채널-모델`·`채널-양보-명세` 가 옛 심볼을 인용하고 있었다(37곳). 안 고치면 다음 세션이
없는 심볼을 찾는다.

### Step 3 — 레인 용량을 낱말로 세운다 ✅ 완료 (2026-09-03)

`laneThroughput` · `laneCapOfTier` 를 `beltThroughput.ts` 에 두었다. `determineBeltCount`
는 **안 건드렸다** — 줄 수는 여전히 줄 용량이 정한다(그 사실 자체를 테스트로 잠갔다:
*"레인 용량으로 셌다면 40/s 가 두 줄이 됐을 것"*).

> **`createLinks` 도 안 건드렸다.** 원래 *"공유 줄일 때만 `cap` 을 레인 용량으로"* 라고
> 적었는데, `createLinks` 는 **품목 하나씩** 불린다(`createLinks(flows, item, limits)`).
> 공유는 **품목 둘 사이**의 일이라 그 함수가 볼 수 없다. 그래서 붓기는 그대로 두고,
> 다 부어진 줄들을 **뒤에서 짝짓는다**([shareLanes]) — 붓기 규칙이 하나도 안 바뀐다.

- **관문:** *"이 줄에 얼마까지 실을 수 있나"* 의 답이 **줄이냐 레인이냐**로 갈린다. ✔
- **불변:** *기전* — `determineBeltCount` 의 인자가 안 바뀌었으므로 줄 수는 다른 답을
  낼 수 없다. `shareLanes` 는 `sharedLineId` 만 얹고 `carries`·`from`/`to` 를 안 만진다.

### Step 4a — 공유 자료 ✅ 완료 (2026-09-03)

`Link.sharedLineId` + `shareLanes()` + `physicalLines()` (`module/link.ts`), 테스트 13개
(`link.share.test.ts`). 자격 넷을 판정한다 — 서로 다른 품목 · 같은 티어 · 양을 안다 ·
**각자 ≤ 레인 용량**. 마지막은 최적화가 아니라 물리다(규칙 ③).

**후보를 고르는 것은 호출자다** — 형제 모듈을 알아야 답하는 물음이라 `module/` 이 답하면
축 2 판정이 깨진다.

### Step 4b — 후보 고르기 + 계측 ✅ 완료 (2026-09-03)

`modulePacking` 이 `linkCache` **최종본** 위에서 짝을 짓는다(`shareLanes` 호출).
`flg.report()` 에 `레인공유` 줄이 생겼다 — **배치는 안 바뀐다**(읽는 방출기가 없다).

> **후보 조건이 §4 에 적은 것과 다르다.** 원래 *"같은 (자식→부모) 쌍"* 이라고 적었는데,
> **자식 하나는 출력 품목이 하나뿐이다**(`productOf`) — 서로 다른 품목은 애초에 서로 다른
> 자식에서만 온다. 실제 조건은 이것이다:
>
> ```
> ⑴ 같은 **부모**로 들어간다          물리 줄이 하나라 부모 포트도 하나가 된다
> ⑵ **위 형제와 아래 형제**에서 온다   채널 세로 주행이 합류 칸에 **양옆으로** 닿는다
> ```
>
> ⑵ 가 §5 의 *"뒤 유입 없음"* 을 **기하로 보증한다.** 같은 쪽에서 오면 위쪽이 아래쪽의
> 뒤 유입이 되어 두 레인을 선점하고 아래쪽이 조용히 굶는다(⑤⑦). 그리고 `siblingHalf` 가
> 이미 그 위/아래를 답한다 — **새로 셀 것이 없다.**

> **순서를 한 번 잘못 잡았다.** 처음엔 `linkCache` 를 **만든 직후**에 표시했는데,
> `allocateTree` 가 그 뒤에 `linkCache.set(s.id, r.groups)` 로 **최종본을 갈아 끼운다**
> (쪼개졌으면 토막). 앞에서 표시하면 그 교체가 표시를 조용히 버린다. → 배정 **뒤**로 옮겼다.
> (`tempPlanDocs/CLAUDE.md` 의 *"순서 미확인이 가장 흔하다"* 가 그대로 맞았다.)

**실물 픽스처에서 실제로 발동한다** — `modulePacking.realTree.test.ts`(advanced-circuit
트리)에서 `후보 1 · 짝 1 · 자격미달 0`. n0 의 자식 둘(kr-ec 위 · ec 아래)이 각각 2/s 라
벨트 20/s 의 레인(10/s)에 여유가 있다. **§9 가 물은 "자격을 통과하는 쌍이 몇인가" 의
첫 답이 0이 아니다.**

### Step 4 — 첫 실물: 납품 경로 두 줄을 레인 합류로 한 줄로 (방향)

자격 조건(넷 다 만족할 때만):

```
같은 (자식 모듈 → 부모 모듈) 쌍이다        ← 물리 줄이 하나이므로 양끝이 같아야 한다
둘 다 링크 포트다                          ← §4: 경계 인서터가 떨어지는 포트만
각자 rate ≤ 레인 용량                      ← §2 ③ 의 물리. 넘으면 split belt 가 먼저다
부모 면에 둘이 같은 깊이로 앉을 수 있다      ← 물리 줄이 하나니 깊이도 하나다
```

- (a) **합류 기하 3칸**(§5 (i))을 `deliveryRoute` 가 낸다. 자리는 폭 3이 이미 있는
      곳부터. 합류 칸은 §5 의 **조건 셋**(지상 · 뒤 유입 없음 · 유입은 두 지류뿐)을
      만족해야 한다 — 그 검사가 곧 자리 고르기다
- (b) `beltFlowConflictCell` 에 **허가된 합류** 목록. 가드를 뒤집는 게 아니라, 이미
      그 기하를 찾아내는 판정에 사전을 하나 붙인다 — 목록에 없는 합류는 여전히 오염이다.
      **사전은 `(지류 둘, 그 칸)` 까지 좁게 적는다**(§5 반대 방향 제약) — 넓게 적으면
      허가한 합류가 남의 줄 옆에 앉아 그 줄의 레인을 접는다
- (c) 부모 모듈의 탭 인서터에 **필터**(②)
- (d) 필터를 블루프린트로 내보낸다 — `GridCell` 에 자리가 없고(`types/layout.ts`),
      export 가 안 쓰고(`Toolbar.tsx` 는 name·direction·recipe·modules·quality·
      infinity_settings 만 쓴다), `BlueprintEntity.filters` 는 이미 있다.
      **`use_filters` 필드 유무를 2.0 실물 블루프린트로 확인해야 한다** — 우리 타입에 없다
- (e) 게임데이터에 `filter_count` 를 싣는다(`export-gamedata.lua` 의 inserter 블록에
      없다). **필터를 못 다는 인서터에 필터를 달지 않는다**

### Step 4c — 부모 쪽 실물 ⏸ **전제가 바뀌었다** (2026-09-03 조사)

포트 모델(*"한 셀에 논리 포트 둘"*, 안 A)로 착수하려고 소비처를 전수 조사했다. **포트는
문제가 아니었다** — 그리고 진짜 관문이 한 단계 앞에 있었다.

#### 포트 쪽은 실제로 싸다

`ModulePort` 소비처는 **거의 전부 `chest.id` 로 조회한다**(`portByChestId` ·
`pairedChestIds` · `usedIn` · `modulePerimeterPass` · `perimeter/tracks`). 그래서 안 A 는
*"논리 포트마다 자기 `Container` 객체(자기 id), 물리 상자는 하나만 `chests[]` 에"* 로
성립하고, 품목 키 조회(`p.line.name === item`)가 **전부 그대로 산다.**

> 다만 `usedIn` 때문에 **id 는 반드시 달라야 한다** — 같은 상자 객체를 공유하면 먼저 짝지은
> 품목이 그 id 를 `usedIn` 에 넣어 **나머지 하나가 영영 짝을 못 찾는다.**

#### 그런데 그 앞에서 막힌다 — **배정이 공유를 모른다**

물리 줄이 하나려면 부모의 **면 벨트**도 하나여야 한다. 합류한 납품 벨트가 부모 벽의 한
칸으로 들어오는데, 면 벨트가 둘이면 그 하나가 둘을 먹일 방법이 없기 때문이다.

그런데 배정은 줄마다 따로 잡는다:

```
tryLinkFace   span = beltRowSpan(ctx, face, arms)          ← 줄마다 자기 구간
              claimDepth(table, 깊이, span[0], span[1], owner)  ← 줄마다 자기 주인
```

두 공유 줄은 **면도 깊이도 구간도 각자** 정해진다. 같은 면·같은 깊이에 앉는 일이 있어도
(`g=1` 줄들이 한 깊이를 나눠 쓰는 정상 경우) 구간이 **떨어져 있고**, 물리 줄 하나는 그
둘과 **사이 행까지** 덮어야 한다 — 그 사이 칸은 아무도 공유 줄을 위해 비워 두지 않았다.

**그러므로 4c 의 첫 조각은 포트가 아니라 배정이다:**

```
공유 쌍을 **한 줄로** 계획한다 — 면 하나 · 깊이 하나 · 구간은 **둘의 합집합**
팔은 양쪽 링크에서 모아 앉힌다(각자 자기 품목의 필터 팔)
`claimDepth` 의 주인이 **물리 줄**이 된다 (오늘은 링크)
```

이건 `planner/module` 의 일이고 `depthBudget`(`g` 예산)과도 닿는다 — 공유 줄은 깊이를
**하나만** 먹으므로 예산에서 유리하지만, 구간이 길어져 그 깊이의 남은 자리를 더 먹는다.

#### 안 둘 — 아직 안 골랐다

| | 안 | 값 |
|---|---|---|
| **㉮** | 배정을 공유 인지로 — 공유 쌍을 `tryLinkFace` 에 **한 후보**로 넣는다 | 정공법. `linkPlanner` 의 후보 루프와 `faceTable` 주인 개념을 건드린다 |
| **㉯** | 배정은 그대로 두고, **둘이 우연히 같은 면·같은 깊이에 앉았을 때만** 합친다 | 싸다. 그런데 *"우연히"* 가 몇 번인지 모르고, 사이 칸이 비어 있으리란 보장도 없다 — **측정부터 해야 한다** |

㉯ 를 먼저 재 보는 것이 이 계획의 결에 맞는다(4b 가 이미 *"세고 나서 짓는다"* 를 했다) —
`flg.report()` 의 `레인공유` 에 *"같은 면·깊이에 앉은 쌍"* 을 한 칸 더 세면 답이 나온다.

---

### Step 5 — 진단이 레인을 안다 (방향)

`analysis/beltFlow.ts` 의 `capacity` 를 레인별로 가른다 — 오늘은 *"한 벨트 = 1품목
가정"* 이라 두 품목을 보면 `(혼합)` 이라고만 적는다(`docs/UI/belt-flow-inspection.md`
§알려진 한계). 그 표시가 **정상**이 되는 첫 경우가 이 기능이다.

③④⑤⑥이 있으니 분석기가 레인을 **셀 수 있다** — 다만 **칸마다 유입 개수를 먼저
세야** 한다(⑥). 개수가 규칙을 고르기 때문이다:

```
유입 1 · 옆   곡선     좌/우 보존
유입 1 · 뒤   직진     좌/우 보존
유입 ≥2       접힘     뒤에서 온 것만 보존 · 옆에서 온 것은 닿은 쪽 레인으로
지하 점프     보존     ④
```

오늘 `beltFlow.ts` 는 상류를 역방향 BFS 로 모으지만 **유입을 세지는 않는다** — 곡선과
사이드로드를 안 가른다. 그걸 가르는 것이 이 단계의 실질이다. `flg.check()` 에 불변식
하나: *"한 물리 줄에 실린 품목은 둘 이하이고, 품목마다 ≤ 레인 용량"*.

⑦ 때문에 **포화 판정도 레인별이라야 정직하다** — 줄 전체로 45/s 가 안 찼어도 한 레인이
22.5 에 닿으면 거기로는 아무것도 못 올라탄다. 오늘의 `saturated` 플래그는 줄 단위라
그 상황을 **정상이라고 말한다.**

### Step 6 — 「가까운 레인 드랍」 플래그 (방향)

`debugFlags.ts` 에 `AUTO_LAYOUT_NEAR_LANE_DROP` 을 넣고 `AutoLayoutDebugTab.tsx` 에
토글을 단다(`COORD DUMP`·`못 쪼개기` 와 같은 꼴 — 켜고 끄며 `flg.report()` 를 비교).

- **관문:** §4 의 결론(*"인서터만으로는 모듈 안에서 두 레인을 못 채운다"*)을 **끌 수
  있게** 한다. 켜지면 같은 쪽 두 팔이 두 레인을 채우고, 그러면 모듈 안에서 레인을
  만들 수 있고 **출력(collect) 레인까지** 열린다 — §8 이 안 푼다고 적은 그것이다.
- **전제(확인 필요):** 켰을 때 나오는 배치가 **게임에서 성립해야** 한다. 무엇으로
  가까운 레인에 떨구는가:
  - 프로토타입 `allow_custom_vectors`(= *"pickup and insert position can be set
    run-time"*)가 참인 인서터만 `drop_position` 을 바꿀 수 있고, **바닐라는 거짓**이다.
  - `BlueprintEntity.drop_position` 은 우리 타입에 **이미 있다**. 없는 것은
    게임데이터 쪽 — `export-gamedata.lua` 가 `allow_custom_vectors` 를 안 싣는다.
  - 그래서 이 플래그는 **`allow_custom_vectors` 를 실은 뒤**라야 정직하다. 그 전에는
    *"이 배치는 바닐라에서 안 된다"* 를 화면이 말해야 한다.
- **불변:** 플래그가 꺼진 배치는 이 단계 전후로 **같은 갈래를 탄다** — 켜야만 새
  분기가 열린다(`AUTO_LAYOUT_LINK_LADDER` 와 같은 관용구).

---

## 8. 이 계획이 **안 푸는** 것

- **출력(collect) 레인** — §4 가 기하로 기각했다. Step 6 의 플래그를 켜면 열리지만,
  그 위에 배치를 얹는 것은 이 계획 밖이다(플래그는 **문만 연다**).
- **known-limits §2 의 「같은 품목 합류」** — 이 계획은 §2 를 **안 닫는다.** 두 항목이
  같은 이름("belt mixing") 아래 있을 뿐 기전이 다르다.
- **3품목 이상 · splitter · 유체** — 폴더 `CLAUDE.md` 의 "하지 않는 것".
- **지하벨트 끝에서의 사이드로드** — §5 가 제약으로 막는다. 답을 몰라도 되게 조인 것이지
  불가능하다고 판정한 것이 아니다.
- **레인을 쓸지 말지의 선택 정책** — Step 4 는 *"자격이 되면 합친다"* 로 시작한다.
  언제 **안** 합치는 게 나은지(예: 나중에 한쪽 양만 늘면 도로 갈라야 한다)는 실물
  트리에서 합류 건수를 세어 본 뒤의 일이다.

## 9. 이게 끝나면 열리는 문

- **면 깊이 부족(`known-limits` §10)이 완화된다** — 저율 입력 둘이 깊이 하나를 나눠
  쓰면 *"자리가 모자라 못 앉는다"* 는 거절이 그만큼 준다.
- **채널 폭이 준다** — 트랙 하나에 두 품목. 다만 합류 칸이 **한 행에서 폭 3**을 먹으므로
  이득은 *"줄이 길수록"* 커진다(§5).
- **`(혼합)` 이 결함이 아니라 상태가 된다** — 벨트 검사·`flg.check` 가 레인을 아는
  순간, 수동 배치한 혼합 벨트도 처음으로 정직하게 읽힌다.
- **`docs/factorio/belt-lane-semantics.md` 가 남는다**(Step 1) — 계획이 폐기돼도, 다음
  사람이 사이드로드를 다시 "오염"으로만 읽지 않는다.

> **문이 열려도 좁다는 것을 함께 적어 둔다.** ⑦ 때문에 합류는 **여유가 있을 때만**
> 쓸 수 있다 — 두 품목이 각자 레인 하나에 넉넉히 들어갈 때뿐이고, 한쪽이 22.5 에
> 가까워지면 그 자리는 애초에 후보가 아니다. 그래서 이 기능이 갚는 것은 **저율 품목이
> 많은 트리**이지 포화된 트리가 아니다. 실물에서 자격을 통과하는 쌍이 몇이나 되는지는
> Step 4 를 켜고 세어 봐야 안다 — **그 수가 이 계획의 값이다.**
