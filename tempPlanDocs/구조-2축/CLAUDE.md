# 구조-2축 — 공통 컨텍스트

> 상태: **계획 1 승인 대기** · 계획 2·3·4 는 방향만

**폴더는 관심사가 정하고, 파일 이름은 일의 종류가 정한다.** 종류의 정의와 전수 분류는
[docs/work-kinds.md](../../docs/auto-layout/common/work-kinds.md) 가 단일 출처다(코드와 함께 사는 사실).
여기 있는 것은 **아직 안 한 일** — 목표 트리와 이관 순서다. 전체 설계는 [구조-2축.md](구조-2축.md).

```
1 · 도형 단일 출처   계획과 방출이 같은 도형 함수를 부른다      ← 확정.  1-도형-단일출처/
2 · 종류로 가르기     큰 파일을 종류 경계에서 자른다             ← 방향
3 · 폴더 이관        planner/execution/module → 관심사 폴더       ← 방향
4 · import 규칙      산문 규칙을 eslint 로 옮긴다                ← 방향
```

**순서가 강제된다.** 2 는 1 이 없으면 `emitModule` 을 못 자르고(도형과 찍기가 한 함수에 있다),
3 은 2 가 없으면 파일을 통째로 옮기는 일이라 이름이 여전히 거짓말한다.

## 왜 이 순서인가 — 이관을 먼저 하면 안 되는 이유

폴더를 먼저 옮기면 **계획서·docs 인용 수백 곳이 한 번에 깨지고**, 그 대가로 얻는 것은
경로 문자열뿐이다. 반대로 1·2 는 **파일 안에서** 끝나므로 인용이 안 깨지고, 3 에 도달할 때는
옮길 것이 이미 종류별로 갈려 있어 이동이 기계적이다.

## 인터뷰에서 확정된 것 (2026-09-12) — 다시 묻지 않는다

| | |
|---|---|
| **독자** | 몇 달 뒤의 작성자 **+** Claude 세션. 코드 형태는 하나로 수렴하고, 갈리는 것은 *길잡이·경고* 한 층뿐 → 그 층은 코드 밖(`CLAUDE.md`)으로 |
| **범위** | `src/autoLayout/` 안쪽 + auto-layout 개념 문서 + 용어사전. UI·factorio 는 제외(타입 파일 1개 이동만 예외) |
| **주석** | **목표는 주석이 필요 없을 만큼의 리팩토링.** 당장 어려우면 당장 할 수 있는 것부터 |
| **용어사전** | 개념 사전 형태 유지, **청소만** |
| **의존** | 스토어 조회를 진입점 한 곳으로 모아 **주입** + 게임데이터 타입을 `UI/store/gameDataStore` 밖으로 파일 1개 이동(재수출로 UI·factorio 무수정) |
| **시점** | 실행 시 계획서·docs 인용을 **같은 커밋에서** 갱신. 이후 기능 작업은 새 구조 위에서 |

## 착수 전 반드시 읽을 것

| 무엇 | 왜 |
|---|---|
| [docs/work-kinds.md](../../docs/auto-layout/common/work-kinds.md) | **종류의 정의와 판정.** 이 계획 전체가 그 축 위에 서 있다. §7 의 결함 넷이 1·2 의 대상 |
| [docs/code-folders.md](../../docs/auto-layout/common/code-folders.md) | **오늘의 폴더가 무엇을 말하나.** 3 이 끝나면 이 문서가 새 트리의 단일 출처가 된다 |
| [docs/pipeline-lifecycle.md](../../docs/auto-layout/common/pipeline-lifecycle.md) | 시대 여섯. **새 트리는 시대를 폴더로 안 쓴다** — 그 이유가 §"정직하게 잃는 것" |
| [docs/CLAUDE.md](../../docs/CLAUDE.md) 판정표 | 문서와 코드가 어긋날 때. **코드가 개념에 미달한 것은 문서를 고치지 않고 결함으로 적는다** |
| `planner/module/linkPlanner.ts` — `portCells` · `splitByTable` · `beltRowSpan` | 계획 쪽 도형. D1 의 한쪽 |
| `execution/module/emitModule.ts` — `emitOutputLinks` · `emitInputLinks` 의 `path`/`span` | 방출 쪽 도형. D1 의 다른 쪽 |

## 하지 않는 것 (전 계획 공통)

- **배치 결과를 안 바꾼다.** 좌표 덤프 전후가 같아야 한다. 도형을 합치는 순간 달라지면
  그것은 성공이 아니라 **D1 이 실제로 갈려 있었다는 발견**이고, 그때는 멈추고 보고한다.
- **`manualEdit/` 를 안 건드린다** — 호출자 0, 타입검사·테스트 제외. 재구현 계획이 따로 있다.
- **UI·factorio 를 안 건드린다** — 게임데이터 타입 파일 1개 이동만 예외(계획 2).
- **모듈 파이프라인의 전략 이름을 안 정한다** — 사용자가 보류한 결정이다.
- **`channel` 과 `perimeter` 의 트랙 경쟁을 안 가른다** — `materializeChannelGeometry` 가 둘을
  한 번에 훑는 이유가 있다(같은 트랙 풀). 가르려면 그 경쟁을 먼저 풀어야 한다.
- **탐색(`dijkstraWithJumps`)을 안 없앤다.** 종류 셋이 안 갈라진 자리로 **표시만** 한다.
