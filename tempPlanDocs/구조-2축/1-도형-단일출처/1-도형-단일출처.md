# 계획 1 · 도형 단일 출처 — 계획과 방출이 같은 함수를 부른다

> 상태·미확인·이 계획 고유의 맥락은 [CLAUDE.md](CLAUDE.md), 전체 설계는 [../구조-2축.md](../구조-2축.md).

## 1. 문제 — 도형이 두 벌이다

한 링크 그룹이 면에 앉으면 그 배정은 **칸을 여럿 먹는다**: 좌석(d1) · 벨트(d`clusterBeltDepth`) ·
포트 인서터와 상자(두 칸) · gap 이면 반출 줄까지. 그 칸이 어디인지를 **두 곳이 각자 계산한다.**

| 어디 | 무엇을 계산하나 | 무엇에 쓰나 |
|---|---|---|
| `planner/module/linkPlanner.ts` | `beltRowSpan`(행 범위) → `portCells`(포트 칸 둘) → `splitByTable`(표 안 / 기둥 밖) | 장부에 **청구**하고 충돌을 검사한다 |
| `execution/module/emitModule.ts` | `topT`/`botT` → `path`(반출 · 올라오기 · 수집) → `span`(포트 두 칸 포함) | 실제로 **셀을 놓는다** |

**둘이 갈리면 조용히 깨진다.** 계획이 안 센 칸에 방출이 놓으면 남의 자리를 밟고, 계획이 센
칸을 방출이 안 쓰면 그 자리가 낭비된다. 코드가 이미 그 위험을 알고 있다 —
`splitByTable` 의 주석은 *"검사와 청구가 **같은 함수**를 부르게 해서 둘이 갈리지 않게 한다.
갈리면 배정이 못 본 다툼이 방출에서 터진다"* 라고 적는다. **그 원칙이 계획–방출 사이에는
적용돼 있지 않다.**

지금 둘을 붙들고 있는 것은 `portCells` 주석의 줄 번호(`emitModule.ts:66`·`:214`·`:381`)뿐이다.
→ [work-kinds §7 D1](../../../docs/auto-layout/common/work-kinds.md)

### 왜 이렇게 됐나 — 계획 쪽 도형이 **장부를 읽는다**

`beltRowSpan` 은 표에서 **빈 좌석을 앞에서부터** 집어(`freeSeatRows`) 행 범위를 구한다.
즉 계획 쪽 도형은 *"아직 안 정해진 배정"* 을 전제로 도형을 **미리 그려 보는** 함수다.
방출 쪽은 이미 확정된 `slots` 로 같은 도형을 다시 그린다. 입력이 달라서 함수를 공유할 수가 없다.

**그래서 순서가 이렇게 된다:** 장부 읽기를 정책으로 올리고 → 도형은 **확정 배정만** 받게 하고
→ 그러면 두 곳이 같은 함수를 부를 수 있다.

## 2. 어떻게 하나 — 네 단계

### 1단계 · 대조 계측 (동작 변화 0)

두 도형이 실제로 같은 답을 내는지 **먼저 잰다.** 이 저장소가 이미 쓰는 방법이다 —
`endsDisagree`·`endsCoarse` 가 옛 판정과 새 판정을 나란히 돌려 대조한다.

```
계획 쪽   commitLinkFace 가 **청구한 칸을 그대로 받아 적는다**(LinkFacePlan.claim, 순번)
          → placeLinkSeats 가 slots 와 **같은 덧셈**으로 t 좌표로 옮긴다(LinkSeats.claimT)
방출 쪽   방출기가 만지는 좌표를 faceCell 의 역으로 (깊이, t) 로 되읽는다(shapeTrace)
대조      링크마다 그 자리에서 onlyPlan / onlyEmit 을 센다 — 신원으로 짝지을 필요가 없다
```

> **유도가 아니라 「받아 적기」인 이유**(2026-09-12 착수 시 변경). 계획 쪽 칸을 새 함수로
> **다시 유도하면** 그것이 곧 3단계의 산출물이고, 그러면 계측이 *"옛 계획 vs 새 함수"* 를
> 재게 된다. 지금 물어야 하는 것은 *"오늘 두 곳이 같은 답을 내나"* 다. 그래서 계획 쪽은
> 장부에 **실제로 청구한 것**을 그대로 적는다.

- 기록은 `debug/runStats.ts` 에 카운터 하나(`recordShapeDiff`) — **이름 규약 `record*` 를 따른다.**
- `flg.report()` 에 한 줄 — `도형대조  일치 N · 계획만 a · 방출만 b`.
- 계측 코드에 **관측 전용** 표식 주석을 단다(*"지워도 배치가 안 바뀐다"*).

### 2단계 · 실측 — **멈춤 게이트**

실게임 데이터로 여러 레시피를 돌려 대조 수를 본다(`flg.run()` → `flg.report()`).

| 결과 | 다음 |
|---|---|
| 전부 0 | 3단계로. 합쳐도 배치가 안 바뀐다는 것이 **측정으로** 보장된다 |
| 0 이 아니다 | **멈추고 보고한다.** 불일치의 내용이 곧 [CLAUDE.md](CLAUDE.md) 의 미확인 셋에 대한 답이다 |

#### 2단계 결과 (2026-09-12 · 픽스처 사전 재기) — **0 이 아니다. 3단계 보류**

브라우저 실측 전에 픽스처로 먼저 쟀다(`module/shapeProbe.measure.test.ts`, 임시).

```
링크 24 · 일치 21 · 계획만 4칸 · 방출만 6칸 · 뺀 것 1(gap)
```

**어긋난 셋이 두 가지 원인으로 갈린다.**

##### F1 · 합류 이끄는 줄의 **기둥 밖 세 칸**이 어느 장부에도 없다 (2건)

```
c→p:x#0 [이끔] W   방출만  d3:-1(합류 칸) d3:-2(포트 인서터) d3:-3(포트 상자)
```

`commitLinkFace` 의 `mergeLead` 분기가 *"합류 칸·포트는 표 밖이라 청구할 것이 없다"* 고
적고 실제로 안 적는다. **그런데 비합류 경로는 기둥 밖 칸도 `ctx.outside`(기둥 밖 칸 장부)에
올린다** — 합류 리드만 그 장부를 우회한다.

오늘은 `ctx.outside` 가 **결정을 안 하고 대조만** 하므로(A1) 실질 피해가 없다. 그러나
`tempPlanDocs/구간-밖-주행/` 의 A2 가 그 장부에 결정권을 주는 순간 **이 구멍이 그대로 결함이
된다** — 합류 칸 셋이 남의 자리로 잡힐 수 있다.

##### F2 · 합류 여부를 **계획과 방출이 다른 근거로 판정한다** (1건)

```
c→p:x#1 [재사용] E   계획[d1:6 d1:7 d2:6 d2:7 d3:6 d4:6]   방출[d1:6 d1:7]
                     → 계획만 d2:6 d2:7 d3:6 d4:6
```

같은 짝의 첫 줄(`#0`)은 좌석 0·1·3·4 에 앉고 벨트를 `d2:0..4` 에 깔았다. 둘째 줄(`#1`)은
좌석 6·7 인데 **자기 벨트를 청구까지 해 놓고 방출에서는 첫 줄의 벨트에 얹혔다.** 근거가 다르다:

```
계획   `sharedTo` 에 첫 줄이 등록됐고 `seatOnSharedBelt` 가 성공했을 때만 공유한다
       (실패하면 `g.sharedLineId = undefined` 로 **짝을 푼다**)
방출   `group.sharedLineId` 가 있으면 **무조건** 먼저 나온 벨트에 얹는다
```

그래서 두 가지가 동시에 일어난다.

1. **유령 예약** — 청구된 `d2:6 d2:7 d3:6 d4:6` 네 칸을 아무도 안 쓴다. 그 면의 다른 줄이
   그 자리를 못 쓴다.
2. **조용한 굶주림** — `#1` 의 팔은 `d1:6`·`d1:7` 에 서서 `d2:6`·`d2:7` 을 집어야 하는데
   그 칸에 벨트가 없다(첫 줄의 벨트는 `d2:0..4` 까지다). 겹침도 미배치도 아니라 **아무도
   못 알아챈다** — `gapExitSidesFromPlans` 주석이 말하는 바로 그 종류다.

#### 2.5단계 · 게이트 해소 (2026-09-12 · 사용자 결정 둘)

| # | 결정 | 고른 것 |
|---|---|---|
| **가** | F2 를 누가 고치나 | **방출이 계획을 따른다** — 역할을 `LinkFacePlan` 에 실어 보낸다 |
| **나** | F1 의 세 칸 | **장부에 올린다** — 합류 리드도 `ctx.outside` 를 쓴다 |

**가 — 판정 주체를 하나로.** 배정이 역할을 정하고 방출은 그 값만 본다.

```
LinkFacePlan.mergeRole   "lead" | "follow"   싣는 쪽 합류에서 이 줄이 무엇인가
LinkFacePlan.sharesBelt  boolean             집는 쪽에서 첫 줄의 벨트에 얹혔나
```

방출의 `canMerge`·`followed`·`reuse` 가 전부 이 두 값에서 나온다 — 신원(`sharedLineId`)과
기하를 **다시 판정하지 않는다.** 배정 쪽도 커밋 직전에 `leadNow()` 로 짝을 다시 확인한다:
큐를 세울 때의 `inPair` 는 그 뒤 짝이 풀려도 참으로 남아서, **합칠 상대가 없는데 합류 도형을
청구하는** 같은 부류의 어긋남을 만들고 있었다.

**나 — 세 칸을 `ctx.outside` 에.** `mergeLead` 분기가 합류 칸·포트 둘을 등록한다. 셋 다
구성상 기둥 밖이라 표(FaceTable)가 아니라 그 장부가 맡는다 — 비합류 경로가 `splitByTable` 로
하는 것과 같은 규약이다. 오늘 그 장부는 대조만 하므로 **이 등록만으로는 배치가 안 바뀐다.**

##### 결과

```
링크 24 · 일치 24 · 계획만 0 · 방출만 0 · 뺀 것 1(gap)      ← 게이트 통과
안전망 netTrips 0 · endsDisagree 0 · endsCoarse 0
타입 0 · 785 테스트 중 783 통과(기존 실패 2 — trunkPipe 유체 면)
```

**배치가 실제로 바뀐 곳이 하나 있다(의도한 교정).** F2 자리에서 둘째 줄이 이제 **자기 벨트와
포트를 놓는다** — 예전엔 남의 벨트에 얹혀 팔 둘이 벨트 없는 칸에 서 있었다. 합류 자체는
그대로 산다: 짝이 성립하는 자리에서는 `[이끔]`·`[따름]`·`[얹힘]` 이 전부 그대로 난다.

> **브라우저 실측이 남았다.** 픽스처는 24 링크뿐이다. `flg.run()` → `flg.report()` 의
> `도형대조` 줄이 실게임 데이터에서도 0 인지 확인한 뒤에 3단계로 간다.

### 3단계 · 도형을 한 곳으로

새 파일 `src/autoLayout/module/linkShape.ts` — **오늘 트리 안에 둔다**(폴더 이관은 계획 3).
`module/` 이 맞는 자리인 이유: 모듈 안쪽 도형이고, `planner/` 와 `execution/` 양쪽이 아래로
내려다보는 위치라 계층이 안 뒤집힌다.

```ts
/** 확정된 배정 하나가 먹는 칸 — 좌표가 아니라 (면, 깊이, 면 위 위치)다. */
export interface ShapeCell {
  kind: "seat" | "belt" | "portInserter" | "portChest" | "corner";
  face: PortFace; depth: number; t: number;
}
/** 배정 → 칸. **장부를 안 본다**(입력이 이미 확정된 배정이다). */
export function linkShapeCells(seated: LinkSeats, geom: { count; machine }): ShapeCell[];
```

- **계획 쪽**(`commitLinkFace`·검사)은 `ShapeCell` 을 장부 청구에 쓴다 — `portCells` 는 지운다.
- **방출 쪽**(`emitOutputLinks`·`emitInputLinks`)은 같은 목록을 `faceCell` 로 절대 좌표에 얹고
  `cellBuilder` 로 찍는다 — `path`/`span` 의 도형 계산은 지운다.
- `tryLinkFace` 의 **예측**(아직 안 앉은 후보의 행 범위)은 정책 쪽에 남는다. 도형이 아니라
  *"이 후보를 앉히면 어떤 배정이 되나"* 이기 때문이다. 그 산출을 `linkShapeCells` 에 넣어
  같은 칸 목록을 얻는다 — **예측과 확정이 같은 도형 함수를 쓰게 된다.**

### 4단계 · 계측을 지운다

`recordShapeDiff` 와 그 배관, `flg.report()` 의 한 줄을 지운다.
`work-kinds.md` §7 의 **D1 을 지우고**, D2(방출이 고른다)·D3(모순 주석)이 함께 해소됐는지 본다.

## 3. 대안 검토

| 안 한 것 | 왜 |
|---|---|
| **바로 합친다**(계측 없이) | 두 도형이 미세하게 다르면 합치는 순간 배치가 바뀌는데, 그때는 *"리팩토링이 깨뜨렸다"* 와 *"원래 갈려 있었다"* 를 구분할 수 없다 |
| **플래그 뒤에서 새 경로** | 도형은 계획·방출 양쪽이 부르는 것이라 플래그를 걸면 **네 조합**이 생긴다. 계측은 조합을 안 늘린다 |
| **방출을 계획 쪽으로 흡수** | 방출에는 도형 말고 찍기(엔티티 이름·방향 규약)도 있다. 통째로 옮기면 종류가 다시 섞인다 |
| **유체(`emitTrunkPipe`)까지 포함** | 유체는 배정을 안 한다 — `fluid_boxes` 가 정한 답을 받아쓴다. 도형의 입력이 배정이 아니라 프로토타입이라 성격이 다르다. 이번 범위 밖 |

## 4. 실패하면 어떻게 되나

- 1단계는 관측뿐이라 실패해도 배치가 안 바뀐다.
- 3단계에서 좌표 덤프가 달라지면 **되돌린다**(커밋 하나로 유지한다).
- 합치는 도중 `exitDepth`·레인 합류 둘째 줄처럼 한쪽만 아는 값이 나오면, **그 값을 도형의
  입력으로 올린다** — 도형 안에서 장부나 배정을 다시 읽지 않는다(그러면 D1 이 재발한다).

## 5. 구현 위치

| 단계 | 파일 | 심볼 |
|---|---|---|
| 1 | `planner/module/linkPlanner.ts` | `portCells` · `splitByTable` · `beltRowSpan` |
| 1 | `execution/module/emitModule.ts` | `emitOutputLinks` · `emitInputLinks` 의 `path`·`span` |
| 1 | `debug/runStats.ts` · `debug/report.ts` | `recordShapeDiff` + 보고 한 줄 |
| 3 | `module/linkShape.ts` (신규) | `ShapeCell` · `linkShapeCells` |

## 6. 검증

```powershell
npx tsc -p tsconfig.app.json --noEmit
npx vitest run                          # 기준선: 62파일 784테스트 (기존 실패 2 — trunkPipe 유체 면)
```

여기에 더해 **좌표 덤프 전후 비교**가 3단계의 게이트다. 테스트 통과는 *"그 코드가 실행됐다"* 를
뜻하지 않는다 — 1단계 계측이 실제로 불렸는지(대조 수가 0보다 큰지)를 먼저 확인한다.
