# 구조 2축 — 폴더는 관심사, 파일은 일의 종류

> 상태·공통 컨텍스트·하지 않는 것은 [CLAUDE.md](CLAUDE.md) 가 든다. 여기는 **목표와 순서**다.

## 1. 무엇을 고치려는가

세 가지가 같은 뿌리에서 나온다.

| 증상 | 실측 |
|---|---|
| **파일을 찾으려면 두 축을 알아야 한다** | `module` 이라는 이름이 세 폴더에 있고, `link` 는 폴더(`planner/link/`)와 파일(`module/link.ts`)이 동명 |
| **변경이 폴더를 넘나든다** | 2026-07-26 이후 2~12파일 커밋 61개 중 **한 폴더에서 끝난 것은 15개**. 함께 바뀐 파일 쌍의 74%가 경계를 넘는다. 조율자 둘을 빼고 폴더 기준을 바꿔 다시 세면 국소율이 26% → 58% 로 오른다 |
| **거대 파일** | 결정 종류를 셋 이상 겸한 파일 일곱이 **전부 500줄 이상** → [work-kinds §5](../../docs/auto-layout/common/work-kinds.md) |
| **거대 함수** | 200줄 넘는 함수 **여덟**. `packModuleTree` **868** · `runModulePipeline` **714**. 60줄 넘는 *중첩* 함수는 셋뿐(최대 87)이라 **본문이 그냥 길다** — 파일만 옮기면 이 수는 하나도 안 준다 |

셋 다 **축이 하나 모자라서** 생긴다. 오늘의 폴더는 *"무엇에 대한 일인가"* 와 *"계획인가 실행인가"* 로
나뉘는데, 실제로 파일 안을 가르는 선은 **일의 종류**다.

## 2. 목표 — 규칙 세 줄

1. **폴더 = 관심사.** `tree` · `module` · `link` · `channel` · `perimeter`, 관심사가 없는 것은 `shared`.
2. **파일 = 종류.** `types`(타입) · `arith`(셈) · `ledger`(장부) · `shape`(도형) · `policy`(정책) ·
   `emit`(찍기) · `build`(조율) · `late`(늦은 결정).
3. **한 종류가 300줄을 넘으면 그 파일을 폴더로 승격**한다(`module/shape/`).
   근거는 실측이다 — 종류 하나만 담은 파일의 최대가 335줄이었다(타입·조율 제외).
4. **함수는 종류로 먼저, 시점으로 다음.** ①**종류가 바뀌는 곳**에서 가르면 조각이 다른
   파일로 간다(규칙 2 를 함수 안으로). 그러고도 한 종류가 크면 ②**단계마다 아는 것이
   달라지게** — 조각 하나는 *"무엇을 알고 들어와 무엇을 알고 나가나"* 로 정의된다.
   길다는 것 자체는 자를 이유가 아니고, **붙은 두 블록이 같은 것을 알면 경계가 아니다.**
   **②가 답을 안 주면 안 가른다**(탐색이 그렇다 — 아는 것이 안 자란다).
   축은 함수마다 다르다 — 좌표는 `packModuleTree` 에서 그 변화가 나타난 **형태**일 뿐이다.
   판정·실측·적용은 [2-종류로-가르기/](2-종류로-가르기/) §3.2.

## 3. 목표 트리 — **파일 배정 확정**(2026-09-13, 호출 사슬 실독)

```
autoLayout/
├ run.ts                 조율 — 시대 ①→⑥ 이 이 한 파일에
├ shared/                관심사가 없는 것
│   types.ts               타입 — Container · Port · PlacedCell · Area · Dijkstra*
│   issue.ts               타입 — LayoutIssue
│   gamedata.ts            어댑터 — BuildSpec · 머신 픽커. 게임데이터를 보는 유일한 층
│   arith.ts               셈 — 벨트·인서터 처리량, 레인 용량, directionToVector
│   grid.ts                도형(공용) — faceCell · segment · bbox · buildOccupancy
│   route.ts        ⚠      탐색 — 장부·정책·도형 셋이 **안 갈린 자리**. 찍기도 이걸 읽는다
│   cells.ts               찍기 — cellBuilder
│   frames.ts              도형 — 좌표 프레임 넘기 (areaUnification)
├ tree/                  ① 무엇을 몇 대  ·  ③ 트리를 어떻게 앉히나
│   types.ts               타입 — NodeSpec · PackConfig · PackResult · DeliverySpec
│   arith.ts               셈 — recipeTree · 머신 수 산정 · toModuleInput
│   shape.ts               도형 — 세로 누적 · colX · 절대 좌표 변환
│   build.ts               조율 — packModuleTree 뼈대 (①생성 → ⑦절대좌표)
├ module/
│   types/          ★★     타입 — 규칙 3 으로 폴더 승격.  **✅ 계획 2 Step 1 로 섰다**(2026-09-13)
│     line.ts                무엇을 나르나 — IoLine · PlannedLine · Link · SupplyCapacity
│     seat.ts                어디에 앉나  — LinkFacePlan · LinkSeats · DepthShortage · LinkFaceStage
│     module.ts              무엇을 받고 내나 — ModuleInput · GeneratedModule · ModulePort · BeltTerminus
│   arith.ts               셈 — trunkEndKey · flowEnd(✅ Step 1) + 팔 개수 · 묶음 g
│   ledger.ts              좌석표 + 기둥 끝 + 기둥 밖 칸
│   shape.ts        ★      배정 → 먹는 칸 · 셀.  계획과 방출의 단일 출처
│   policy.ts              배정 순서 · 선호 면 · 넘김 · 쪼갬
│   emit.ts                shape 산출을 Area 에 찍는다
│   late.ts                늦은 결정 — 벨트 종착
│   build.ts               조율 — generateModule
├ link/
│   types.ts               타입 — DeliveryConfig · DeliveryRoute · DeliveryResult
│   arith.ts               흐름 배분 · seatIsBeltFeeder
│   policy/         ★★     규칙 3 으로 폴더 승격 — 짝짓기 · 레인 공유 · 배선 형태 · 폴백 선택
│   shape.ts               납품 경로 도형        emit.ts   경로 → 셀
│   build.ts               조율 — routeDeliveryRoutes
├ channel/
│   types.ts               타입 — Delivery/ExportInput · ChannelGeometryPlan
│   ledger.ts              트랙 장부 · 행 채널 · 백트래킹 배정
│   shape.ts        ⚠      경로 도형 · 계단꼴 · 트랙→x.  **외곽 반출과 같은 트랙 풀**
│   policy.ts              같은 쪽 판정 · 해소 사다리 · 양보
├ perimeter/
│   types.ts               타입 — Exit* · PerimeterExitPlan
│   ledger.ts              직진 장부          shape.ts    포트→바깥 변 · 광선 · wayOuts
│   policy.ts              출구 배정 · 강등(관측 전용 표식 포함)
│   late.ts                살아남은 상자 이사
└ manualEdit/            격리 유지
```

이름이 겹치는 것이 의도다 — `shape.ts` 가 **다섯**(tree·module·link·channel·perimeter)이고,
그 다섯은 폴더가 구분한다. `types.ts` 도 여섯이다.
지금처럼 `module` 이 세 폴더에 흩어지는 것의 반대다.

### 오늘 파일이 어디로 (모듈 관심사)

> **타입은 계획 2 Step 1 이 옮겼다**(2026-09-13) — `planner/module/ioLine.ts`(통째) ·
> `clusterModule`·`linkPlanner`·`planModulePorts`·`link`·`beltTerminus` 의 **여러 층이 함께 읽는**
> 타입 → `module/types/`, `trunkEndKey`·`flowEnd` → `module/arith.ts`. 아래 표의 줄 수는 옮기기
> **전** 값이고, 표는 **그 뒤 남는 것**이 어디로 가나다.

| 지금 | 어디로 | 왜 |
|---|---|---|
| `planner/module/faceTable.ts` | `module/ledger.ts` | 이미 한 종류. 이름만 바뀐다. **옮기면 `types/seat.ts → planner/module/faceTable` 상향 타입 간선이 닫힌다**(Step 1 이 알고 남긴 것) |
| `planner/module/allocateArms` · `depthBudget` | `module/arith.ts` | 자원도 좌표도 없는 셈 |
| `planner/module/linkPlanner.ts` (1027) | **셋으로** — `ledger` · `shape` · `policy` | 후보 정렬·끝 선택은 정책, 포트 칸·행 범위는 도형, 청구는 장부 |
| `planner/module/planModulePorts.ts` (920) | `policy.ts` + `build.ts` | ⓪①③④ 네 단계는 정책의 실행 순서다 |
| `module/clusterModule.ts` (612 → 350) | `build.ts` | 본문은 조율. 타입은 Step 1 이 뗐다 |
| `module/link.ts` (918 → 790) | `arith.ts` + `policy.ts` | 산술·형태 선택이 한 파일에 있다. `Link` 는 Step 1 이 뗐다 |
| `module/clusterLayout` · `moduleTransform` | `shape.ts` | 둘 다 순수 기하 |
| `execution/module/emitModule.ts` (897) | `shape.ts` + `emit.ts` | **계획 1 의 본체** |
| `execution/module/beltTerminus.ts` | `late.ts` | 늦은 결정 |
| `module/fluidPorts.ts` | `shared/gamedata.ts` + `module/policy.ts` | prototype 해석은 어댑터, 회전 선택은 정책 |

### 나머지 네 관심사 (2026-09-13 · 4,020줄을 읽고 채웠다)

[work-kinds §5](../../docs/auto-layout/common/work-kinds.md) 의 *"결정 종류 셋 이상"* 일곱 중
위 표가 덮은 것은 `linkPlanner`·`planModulePorts` 둘뿐이었다. 나머지 다섯이 아래다.
**줄 수는 어림**이다 — 배정의 근거는 줄 수가 아니라 *"무엇을 결정하나"* 다.

#### `modulePacking.ts` (1358) — **다섯 관심사에 걸쳐 있다.** 조율자를 남기고 넷을 내보낸다

| 무엇 | 어디로 | 왜 | 줄 |
|---|---|---|---:|
| `NodeSpec`·`PackConfig`·`PackResult`·`DeliverySpec`·`ModulePlacement`·`RowChannel`·`DeliveryDirective`·`PackChannelGeometry` | `tree/types.ts` | 패킹 **결과**의 어휘. `deliveryRoute`·`modulePerimeterPass`·`moduleWizard`·`perimeter/exits` 가 다 읽는다 | ~226 |
| `packModuleTree` 뼈대(①생성 → ⑦절대좌표) · `allocateTree` · `gen` | `tree/build.ts` | 조율 — 순서대로 부르고 엮는다 | ~280 |
| 레인 공유 짝짓기 · 다이렉트 재붓기 · `productOf` · `siblingHalf` · `linkEndOf` · 납품 짝짓기(2) | `link/policy.ts` | **전부 대안 중에서 고른다**, 그리고 전부 두 모듈의 식별자를 안다 | ~350 |
| 세로 순서 · 행 채널 신원 · 트랙 배정(3) | `channel/ledger.ts` | 공유 자원의 청구. 코드가 스스로 *"전부 좌표가 없다"* 고 적는다 | ~70 |
| 세로 좌표 누적(4) · `colWidth`/`colX` · `absById` · `unionExtent` | `tree/shape.ts` | 좌표를 낸다 | ~250 |
| `materializeChannelGeometry` | `channel/shape.ts` ⚠ | 트랙 index → 절대 x. **외곽 반출도 같이 훑는다**(같은 트랙 풀) — 안 가르는 자리 | ~116 |
| `toModuleInput` · `nsExposureOf` | `tree/arith.ts` | 좌표도 자원도 없는 유도 | ~40 |

#### `channelGeometryPlanner.ts` (771) — 이미 한 관심사. **네 종류로만 가른다**

| 무엇 | 어디로 | 왜 | 줄 |
|---|---|---|---:|
| `DeliveryInput`·`ExportInput`·`GeometryContext`·`Jump`·`DeliveryPlan`·`ExportPlan`·`ChannelGeometryPlan`·`ChannelEndpoint` | `channel/types.ts` | 타입 | ~125 |
| `sameSideOfCut` · 해소 사다리 ①②③ · `tryFlip` · 실패비용 순 정렬(유체→반출→아이템) | `channel/policy.ts` | **핵심 판정 + 순서.** 머리말이 대놓고 *"사다리"* 라 부른다 | ~120 |
| `routeShape`·`staircaseShape`·`columnSwitchShape`·`elbowShape`·`staircaseCells`·`shapeFromCells`·`placeWithJumps`·`cellsOf`·`haloOf`·`conflicts` | `channel/shape.ts` | 좌표만 낸다 | ~300 |
| `planChannelGeometry` 배정 루프(백트래킹 · `placedShapes` 누적) · `tryColumnSwitch` | `channel/ledger.ts` | **상태를 갖는다** — work-kinds 가 이 파일을 장부의 예로 이미 들었다 | ~220 |

#### `perimeterExitPlanner.ts` (593) — **타입이 35%.** 떼면 385줄

| 무엇 | 어디로 | 왜 | 줄 |
|---|---|---|---:|
| `ExitEdge`·`ExitMode`·`ExitPortInput`·`ExitOption`·`ExitContext`·`ExitAssignment`·`PerimeterExitPlan` | `perimeter/types.ts` | `layoutRegions`·`perimeterRouter`·`perimeter/directRay`·`modulePerimeterPass` 가 **타입만** 읽는다(간선 넷이 전부 타입) | ~180 |
| `enumerateOptions`·`directOpt`·`channelOpts` · `planPerimeterExits`(제약 센 곳부터 · 강등 · 강행) | `perimeter/policy.ts` | 후보 나열 + 선호 + 실패 처방 | ~200 |
| `directRayOf` · `laid` 등록 | `perimeter/ledger.ts` | **직진 장부** — 머리말이 *"직진은 여기서 장부에도 오른다"* 로 이미 판정 | ~20 |
| `ExitDemotion`·`ExitBlocked`·`describeBlocked` | `perimeter/policy.ts` 안에 **관측 전용 표식** | work-kinds §4 가 이미 *"읽기 전용 계측"* 으로 세어 뒀다. 종류가 아니라 꼬리표라 파일을 따로 안 만든다 | ~65 |
| `nearerNS` | `perimeter/policy.ts` | 5줄. 따로 안 뗀다 | ~5 |

#### `deliveryRoute.ts` (717) — link 관심사. **여섯 종류가 한 파일에**

| 무엇 | 어디로 | 왜 | 줄 |
|---|---|---|---:|
| `DeliveryConfig`·`DeliveryRoute`·`DeliveryResult` | `link/types.ts` | 타입 | ~98 |
| `routeDeliveryRoutes` 본체 | `link/build.ts` | 조율 | ~160 |
| `routeOneDelivery`(계획 경로 ↔ dijkstra 폴백 선택) · `isContinuous` | `link/policy.ts` | **대안이 둘 있다** | ~75 |
| `portGeometry`·`onSegment`·`buildPlannedChain`·`plannedChainClear` | `link/shape.ts` | anchor+face → 좌표 | ~190 |
| `finishChain`·`finishFluidChain` (`emitItemPath`·`emitFluidPath` 호출) | `link/emit.ts` | 찍기 | ~65 |
| `seatIsBeltFeeder`·`synthPair` | `link/arith.ts` | 답이 하나 — 셈 | ~30 |
| `buildOccupancy(pack)` | `shared/grid.ts` | 좌표 집합 만들기. `containerRouting` 에 **동명 함수가 또 있다**(시그니처만 다름) — 합칠 자리 | ~13 |

#### `containerRouting.ts` (581) — **가르지 않는다.** 주소만 바꾼다

| 무엇 | 어디로 | 왜 | 줄 |
|---|---|---|---:|
| `JumpEdge`·`RouteEdge`·`DijkstraResult`·`DijkstraEndpoint`·`DijkstraInput`·`BeltFlow` | `shared/types.ts` | 타입 | ~117 |
| `dijkstraWithJumps`·`searchWithJumps`·`isJumpAllowed` | `shared/route.ts` ⚠ | **[work-kinds §6](../../docs/auto-layout/common/work-kinds.md) 이 이미 판정** — 장부·정책·도형 셋이 한 함수라야 성립한다. 표시만 하고 안 가른다 | ~300 |
| `collectBeltFlow`·`beltFlowConflictCell` | `shared/route.ts` | 탐색의 충돌 판정. 위와 함께 산다 | ~90 |
| `directionToVector` · `buildOccupancy(area)` | `shared/arith.ts` · `shared/grid.ts` | 셈 · 좌표 집합 | ~25 |

### 이 표가 §3 트리를 세 군데 고친다

| 고칠 것 | 왜 |
|---|---|
| `link/ route.ts ⚠ 탐색` → **`shared/route.ts`** | 소비처가 link 만이 아니다 — `execution/emitPath` 가 `DijkstraResult`·`JumpEdge` 를, `moduleWizard` 가 `directionToVector` 를 읽는다. link 에 두면 **찍기가 link 를 import** 하게 된다 |
| `tree/` 가 `arith.ts` 하나 → **다섯**(`types`·`build`·`shape`·`arith` + 위임) | `packModuleTree` 가 트리 관심사의 본체인데 트리가 레시피 셈 하나뿐이었다 |
| `link/policy.ts` 는 **폴더 승격 대상** | `modulePacking` ~350 + `deliveryRoute` ~75 + 기존 `link/edgeLinks` → 300줄 규칙(§2 규칙 3)에 걸린다 |

> **일곱 파일이 전부 배정됐다.** 계획 2 본체는 이제 *"어디로"* 를 다시 묻지 않는다 —
> 남은 것은 순서와 커밋 단위뿐이다.

## 4. 이 구조가 만드는 import 규칙 다섯

산문으로 적힌 규칙이 검사 가능한 문장이 된다(계획 4).

| 규칙 | 무엇을 지키나 |
|---|---|
| `shape` → `ledger` **금지** | **도형이 자원 상태를 모른다.** 계획과 방출이 같은 함수를 부를 수 있는 근거 |
| `emit` → `policy` **금지** | 찍기는 고르지 않는다 |
| `shared/*` → 관심사 폴더 **금지** | 공용은 위를 모른다 |
| `module` → `link`·`channel`·`perimeter` **금지** | 관심사 경계 |
| 스토어는 `shared/gamedata.ts` **만** | 오늘 `useGameDataStore.getState()` 가 네 곳에 흩어져 있다 |

첫 줄이 핵심이다. 오늘 도형이 두 벌인 이유가 **계획 쪽 도형이 장부를 읽으면서 계산하기**
때문이다(`beltRowSpan` 이 표에서 빈 좌석을 찾아 그 결과로 포트 칸을 구한다). 장부 읽기를
정책으로 올리고 도형은 **배정 결과만** 받게 하면 방출도 같은 함수를 부를 수 있다.

## 5. 정직하게 잃는 것

**트리가 실행 순서를 말하지 않는다.** 폴더를 먼저 보는 사람에게는 실제 손해다.
완화책은 루트에 파일이 `run.ts` 하나뿐이라는 것 — 트리를 열면 폴더들과 그 파일 하나가 보인다.

폴더에 번호를 붙여 순서를 드러내는 안은 **기각했다. 거짓말이 되기 때문이다** —
`module` 과 `link` 는 얽혀 돈다. 링크 하나를 앉히려면 두 모듈의 장부를 함께 봐야 해서
`seatLinkEdge` 가 **링크 축으로 돌면서 모듈 장부를 쓴다**. 번호는 이 사실을 감춘다.

## 6. 순서

| # | 무엇 | 산출 | 동작 변화 |
|---|---|---|---|
| **1** | **도형 단일 출처** — 대조 계측 → 실측 → 추출 → 계측 제거 | `module/shape` 의 씨앗 | **0** (계측 단계) → 합칠 때 0 이어야 함 |
| 2 | **종류로 가르기** — 일곱 파일을 결정 종류로. 타입이 먼저인 것은 **D4 를 닫아 나중 자르기가 안 뒤집히게** 하려는 것(경계를 보이게 하려는 게 아니다). 게임데이터 조회 주입 + 타입 파일 1개 이동 | `module/types/` · 어댑터 층 · 파일 분할 | 0 |
| 3 | **폴더 이관** — 관심사 폴더로. 계획서·docs 인용을 같은 커밋에서 갱신 | 새 트리 · `code-folders.md` 재작성 | 0 |
| 4 | **import 규칙** — 위 다섯을 eslint 로. `CLAUDE.md` 의 해당 산문을 지운다 | 규칙 5 + 문서 축소 | 0 |

## 7. 검증

```powershell
npx tsc -p tsconfig.app.json --noEmit   # 반드시 -p
npx vitest run                          # 기준선: 62파일 784테스트 (기존 실패 2 — trunkPipe 유체 면)
```

여기에 더해 **좌표 덤프 전후 비교**를 각 단계의 게이트로 둔다(`flg.run()` → `flg.report()`).
테스트 통과는 *"그 코드가 실행됐다"* 를 뜻하지 않는다 — 바꾼 분기가 실제로 불렸는지 먼저 본다.

## 8. 계획서를 지울 때

실행이 끝나면 이 폴더는 지운다. 그전에 옮길 것:

- **목표 트리와 import 규칙** → [code-folders.md](../../docs/auto-layout/common/code-folders.md) (새 트리의 단일 출처가 된다)
- **종류의 정의** → 이미 [work-kinds.md](../../docs/auto-layout/common/work-kinds.md) 에 있다. 옮길 것 없음
- **결함 D1~D4 의 해소 여부** → work-kinds §7 에서 지운다(해소된 것만)
