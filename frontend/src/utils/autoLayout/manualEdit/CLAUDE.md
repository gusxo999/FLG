# manualEdit/ — 비활성 구역

**이 폴더의 코드는 실행되지 않는다. 호출자가 0 이다.**

- 타입검사·테스트 **제외** (`tsconfig.app.json` · `vitest.config.ts`)
- 일부 파일(`storeActions.ts` · `sessionSetup.ts`)은 **원본 조각**이라 컴파일되지 않는다 — 정상이다
- 배치 파이프라인은 여기를 **부르지 않는다.** 여기서 뭔가를 import 하려 한다면 잘못 가고 있는 것이다

**수정하지 말 것.** 리팩토링·타입 변경 때 이 폴더는 건드리지 않는다.
낡아도 무방하다 — 산출물은 코드가 아니라 [README.md](README.md) 의 **의도 기록**이다.

## 무엇이었나

자동 배치 결과를 사용자가 마우스로 끌어 고치던 기능(드래그 재라우팅·라우팅 편집).
옛 Dijkstra 탐색 전제 위에 있어 예약 기반으로 바뀐 지금 구조와 맞지 않는다.

- **무엇을 하려던 기능인가** → [README.md](README.md) (의도 8가지 + 알려진 결함)
- **다시 만드는 법** → `tempPlanDocs/수동편집-재구현-계획.md`
