---
tags: []
---

<!--
문서 골격. 복사해서 쓰고 이 주석은 지운다.
- tags 는 README.md 의 태그 규칙표에서 고른다.
- 폴더는 code-folders.md 의 두 축으로 판정한다.
- 절을 다 채울 필요는 없다. **비어 있을 절은 지운다** — 빈 제목이 있으면 다음 사람이
  "여기 뭔가 있어야 하는데" 라고 읽는다.
-->

> **부모 문서:** [wizard.md](auto-layout/wizard.md)
> **관련 문서:** [[다른-문서]] — 무엇을 맡나 · [[또-다른-문서]] — 무엇을 맡나

# 제목 — 결론을 담은 한 줄

> **이 문서를 읽어야 하는 때**
> - `어느/파일.ts` 를 수정할 때
> - `어떤심볼` · `어떤상수` 를 건드릴 때
> - "어떤 증상" 을 조사할 때
> - "이걸 새로 짜자" 는 생각이 들 때 → **§대안 검토를 먼저 읽을 것**

## 0. 한 줄 요약

결론을 먼저. 이 문서를 안 읽어도 이 한 줄은 남게.

## 1. 문제 — 왜 이 결정이 필요했나

무엇이 안 됐나. **증상이 아니라 원인 수준**으로 적는다.

## 2. 어떻게 하나

실제 동작. 코드가 사실이므로 **심볼 이름을 정확히** 쓴다(`rg` 로 찾아올 수 있게).

## 3. 대안 검토 — 왜 다른 걸 안 했나

기각한 선택지와 그 이유. **이게 없으면 다음 사람이 같은 제안을 다시 한다.**
삭제된 코드라면 커밋 해시를 남긴다(`git log -S심볼` 로 되찾을 수 있게).

## 4. 실패하면 어떻게 되나

정직한 실패 경로. 무엇을 skip 하고 어디에 사유가 남나.

## 5. 구현 위치

| 단계 | 파일 | 심볼 |
|---|---|---|
| | | |

## 6. 함정

이름·인상으로 판단하면 틀리는 지점. 실측으로 확인한 것만.
