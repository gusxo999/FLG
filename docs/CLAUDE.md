# docs/ — 설계 지식 베이스

코드만으로는 자명하지 않은 **"왜"** 를 적는 곳이다. 저장소 루트가 Obsidian vault 다.
지도는 [README.md](README.md), 골격은 [_template.md](_template.md).

## 새 지식이 생겼을 때 — 네 질문

| # | 질문 | 예 → 어디로 |
|---|---|---|
| **Q1** | 이 프로젝트 밖에서도 참인가? | 예 → **Factorio 문서** 확인이 먼저다. 우리가 적을 건 *"그래서 우리는 어떻게 하나"* 뿐 |
| **Q2** | **코드를 바꾸면 무효가 되나?** | 예 → `docs/` (코드와 같은 커밋에서 갱신된다) · 아니오 → 메모리(작업 습관·개인 취향) |
| **Q3** | 이미 그걸 다루는 문서가 있나? | 있다 → **그 문서에 넣는다.** 새로 만들지 않는다 |
| **Q4** | 같은 사실이 두 곳에 생기나? | 그렇다 → 한 곳만 서술하고 나머지는 **링크**한다 |

> **Q3 가 가장 자주 틀린다.** 절이 여러 개라서 문서를 나누지 않는다 — **주제가 다를 때만** 나눈다.
> 특히 **앵커 없는 지식**(요구사항·이력)은 떼면 아무도 안 여는 3줄 문서가 된다.
> 앵커 있는 지식(구현 설명)에 **업혀 있어야** 산다.

## 어느 폴더인가

[auto-layout/common/code-folders.md](auto-layout/common/code-folders.md) 의 **두 축**으로 판정한다.
문서가 코드 폴더에 대응하면 **그 거울**로 둔다 — `auto-layout/{common,module,link,channel,perimeter}/`
는 `src/autoLayout/` 의, `factorio/`·`blueprint/`·`UI/` 는 같은 이름의 `src/` 폴더의 거울이다
(2026-08-02 `utils/` 해체, 2026-08-03 `UI/` 신설로 넷 다 거울이 됐다). 대응이 없으면 주제로 둔다.
사전·MOC 는 전역이라 최상위다.

**이 거울이 도달 경로다** — 코드 폴더의 `CLAUDE.md` 가 같은 이름의 문서 폴더를 가리키므로,
그 코드를 건드리면 해당 문서가 시야에 들어온다. 규칙이 아니라 구조다.

## 링크 — 문서끼리는 `[[위키링크]]`

| 형태 | 폴더 이동 | 이름 변경 |
|---|---|---|
| `[[문서이름]]` | **안 깨진다**(이름으로 찾는다) | 깨진다 |
| `](상대경로.md)` | 깨진다 | 깨진다 |

**문서 → 문서는 `[[위키링크]]`, 문서 → 코드는 상대경로**를 쓴다. 2026-08-02 이관에서
위키링크 115 · 상대경로 168군데를 고쳤는데, 전부 위키링크였다면 이름 바꾼 것만 고치면 됐다.

## 파일을 옮기거나 이름을 바꿀 때

옮기기 전에 **인용을 먼저 센다**:

```powershell
rg -F "옛이름" --glob '!node_modules'    # 위키링크 · 상대경로 · 본문 언급 전부
```

Obsidian 의 자동 갱신은 **Obsidian 이 직접 옮길 때만** 작동한다 — git·스크립트로 옮기면
아무것도 안 해 준다. 옮긴 뒤 링크가 실제 파일을 가리키는지 확인한다.

## 문서와 코드가 어긋나면

**코드가 현재 사실이다**. 어긋남을 발견하면 삭제만 사용해 문서를 갱신한다. —
"나중에" 는 오지 않고, 틀린 문서는 다음 세션을 **적극적으로 오도한다**(실제로 `code-folders`
의 옛 `planner/` 정의가 그랬다).

## 다시 제안하지 말 것 — 문서 검증 스크립트

`scripts/check-docs.mjs`(깨진 링크·고아 문서·태그 누락 검사) 안은 **철회됐다**
(2026-07-29 사용자 지시).

**대신 쓰는 것:** 판단형 규칙은 **이 파일이 적시에 로딩되는 것**으로 대체된다 — 사후 검사보다
낫다. 신규 문서의 골격은 [_template.md](_template.md) 가 맡는다. 개명·삭제 시의 인용 확인은
위 §"파일을 옮기거나 이름을 바꿀 때" 의 `rg` 한 줄이다.

**정직하게 잃는 것:** 깨진 링크와 고아 문서의 **자동** 검출. 링크 부패가 실제로 관측되면
그때 재검토한다 — 선제 자동화는 과잉이다.

> **왜 규칙을 안 늘리나.** 이 계획은 초안에서 규범적 진술을 22개 만들었다 — 문서 한 편을
> 위해서다. 규칙이 많으면 안 지켜진다는 것을 진단해 놓고 그 실패를 반복한 것이다.
> **가장 강한 집행은 규칙을 없애는 것이다:** 폴더에 넣으면 자동 로딩되게 만들면
> "읽어라" 라고 말할 필요가 없다.

### 고아 문서 — 2026-07-29 실측

들어오는 간선이 README 표 하나뿐이면 **아무도 못 찾는다.** 당시 참조 2개 이하가 7건이었고,
그중 `module-way-outs` 는 최근 만든 좋은 문서인데 도달 경로가 없었다. 폴더 거울과 중첩
`CLAUDE.md` 가 그 문서들에 간선을 준다 — **새 문서를 만들 때 "누가 여기로 오게 되나" 를
먼저 답한다.**

