---
tags: [auto-layout, placement, routing]
---

# 채널 기하 예약 — 납품·반출 경로의 "같은 쪽 판정"

> **부모 문서:** [auto-layout-wizard.md](../wizard.md) — 위저드 인터페이스
> **관련 문서:** [.s-layer-channel-reservation](s-layer-channel-reservation.md) — 채널 예약(폭 계산)의 기존 설계,
> [.ns-face-relief](../module/ns-face-relief.md) — 같은 갇힘 문제의 N/S 면 측 치료, [.priority-ordering](../common/priority-ordering.md)

> **상태: 구현 완료** (2026-07-09 설계 확정, 같은 날 구현). 통합 장부 =
> `src/autoLayout/planner/channelGeometryPlanner.ts`. **스위치는 없다** — 2026-09-08 에
> off 모드를 통째로 지웠다(아래). 신규 코드는 이 문서의 용어를 쓰지만(§8 대응표) 기존
> 파일명(delivery, lane)의 개명은 장기 과제로 남아 있다. 검증 결과는 §9.
>
> **2026-09-08 — `AUTO_LAYOUT_CHANNEL_GEOMETRY` 와 off 모드 제거.** 껐을 때의 세계에는
> **「경로 예약」이 아예 없었다** — 폭만 예약하고 방출이 탐색으로 길을 찾았다. 그래서
> *"예약된 경로는 항상 방출 가능"* 이라는 철학이 그 모드에서는 성립하지 않았고, 두 모델을
> 함께 이고 갈 값이 없었다. 함께 사라진 것: 옛 폭 계산(`assignTracksLeftEdge` 로 반출
> 세로 구간을 세던 경로 · `ExitOption.interval` · `channelTrackIntervals`), 옛 유체 탐색
> (`routeOneFluidDelivery`), `PackConfig.channelGeometry`. `PackResult.channelGeometry` 는
> **필수 필드**가 됐다.

---

## 0. 한 줄 요약

지금의 [[용어사전#채널 (channel)|채널]] 예약은 **"몇 줄이 지나갈지(폭)"만** 예약하고 **"누가 어느 칸을 쓸지(기하)"는** 예약하지 않는다.
그래서 먼저 깔린 경로가 나중 경로의 자리를 뺏고, 밀려난 외부상자가 결과물 안에 갇힌다.
이 문서는 예약을 **폭 → 기하**로 승격하는 설계다. 핵심 도구는 **같은 쪽 판정**:

> 반출 경로는 채널에 **절단선**을 긋는다. 납품 경로는 두 끝이 절단선의 **같은 쪽**일 때만
> 지상으로 갈 수 있고, **다른 쪽**이면 지상 경로가 기하적으로 존재하지 않는다 —
> 그리고 이 판정은 벨트를 한 칸도 깔기 전에, 숫자 비교만으로 끝난다.

---

## 1. 문제/배경 — 예약의 구멍

증상: kr-electronic-components 모듈 트리에서 kr-glass·copper-cable 의 외부 공급 상자가
[[용어사전#perimeter ring|perimeter ring]](완성된 배치를 둘러싼 테두리)으로 빠져나가지 못하고 **결과물 한가운데 갇힌다.**

원인은 라우터의 실력 부족이 아니라 **예약의 구멍**이다. 현재 파이프라인의 순서는:

1. 채널 폭을 계산해 빈 세로 통로를 예약한다 (`channelPlanner.ts` — [.s-layer-channel-reservation §4](s-layer-channel-reservation.md)).
2. 머신을 배치하고, 자식→부모 벨트(납품 경로)를 **먼저** 깐다.
3. 맨 마지막에 갇힌 상자를 테두리로 빼는 벨트(반출 경로)를 깔려고 시도한다.

1단계 예약은 "이 채널에 벨트 N줄이 들어간다"까지만 보장한다. **N줄이 서로 어떻게 배치돼야
전부 성립하는지는 아무도 조율하지 않는다.** 그 결과 2단계에서 먼저 깔린 납품 경로가
채널의 요지를 차지하고, 3단계의 반출 경로는 탐색([[용어사전#Dijkstra|dijkstra]])을 아무리 잘해도
지나갈 길 자체가 없어 실패한다 — 상자는 갇힌 채로 남는다.

선택지는 둘이었다:

- **(기각) 라우터를 더 똑똑하게** — 우회·폴백 탐색 추가. 실패를 늦출 뿐 구조적 보장이 없고,
  탐색이 늘수록 결과가 입력에 민감해진다. §7 참조.
- **(채택) 예약을 완성** — 채널 예약을 "폭"에서 "기하"로 승격해, **애초에 실패할 수 없게 만든다.**
  S-LAYER 의 기본 철학([[용어사전#S-LAYER|S-LAYER]] = 예약으로 [[용어사전#백트래킹|백트래킹]] 제거)과 같은 방향.

---

## 2. 용어 — 채널을 지나는 두 종류의 경로

이 문서 전체가 네 단어 위에 서 있다. 그림과 함께 하나씩 정의한다.
좌표 규약: **y 는 아래로 증가**한다 (N = 북 = 위 = min y). 채널의 서쪽 벽은 부모 열, 동쪽 벽은 자식 열이다.

### 납품 경로 (deliveryRoute) — 자식이 부모에게 물건을 올려보내는 벨트

자식 머신의 출력 상자에서 나와, 채널을 건너, 부모 머신의 입력 상자로 들어간다.
양 끝의 행(출발 행 = 자식 출력 상자의 y, 도착 행 = 부모 입력 상자의 y)은 포트가 이미 정해 놓았고,
자유롭게 고를 수 있는 것은 **채널 안에서 세로로 달릴 열([[용어사전#트랙 (track)|트랙]]) 하나뿐**이다.
그래서 납품 경로의 모양은 언제나 **계단꼴(staircase)** — 가로·세로·가로 직선 3개, 계단 한 단 — 이다.
좌우·상하 어느 쪽으로 뒤집혀도 여전히 계단이므로 방향과 무관하게 성립하는 이름이다:

```
             W벽(부모 열)      E벽(자식 열)
              │ x1  x2  x3 │
  y=2 (출발행) │  .   ╔═══╪══● 자식 출력 상자   ← ① 가로 진입
  y=3         │  .   ║   .  │                ← ② 세로 주행 (트랙 x2)
  y=4         │  .   ║   .  │
  y=5 (도착행) ●═╪══════╝   .  │                ← ③ 가로 진출
      부모 입력 상자
```

출발 행 = 도착 행이면 ②가 사라져 일자 수평선이 된다(트랙 소비 0).

### 반출 경로 (exportRoute) — 갇힌 상자를 테두리로 빼내는 벨트

외부 공급/흡수 상자가 배치 안쪽에 갇혔을 때, 그 상자를 채널을 따라 **열린 위/아래 끝(N변 또는 S변)** 으로
빼내는 벨트. 벽의 한 점에서 출발해 열린 변까지 가므로 모양은 **한꺾임꼴(elbow)** — 직선 2개 — 이다:
가로 진입 한 번 + 열린 변까지 세로 주행.

### 절단선 (cut)

반출 경로가 채널 위에 만드는 분리선. 반출 경로는 "벽의 한 점 → 열린 변"을 잇기 때문에,
지상 벨트끼리 교차할 수 없다는 사실과 합쳐지면 **채널을 두 쪽으로 가르는 선**이 된다.
그래프 이론의 절단(cut) — "모든 경로가 반드시 건너야 하는 선" — 과 정확히 같은 개념이라 이 이름을 쓴다.

### 같은 쪽 판정 (sameSideOfCut)

납품 경로의 두 끝이 절단선의 같은 쪽인지 비교하는 것. §4 가 본문이다.

---

## 3. 모델 — 경로는 탐색하는 것이 아니라 숫자 몇 개로 그려지는 도형이다

§2 의 그림에서 봤듯이, 채널 안 경로의 자유도는 극히 작다:

| 경로 | 고정된 것 (포트가 결정) | 자유로운 것 (계획기가 배정) | 결과 모양 |
|---|---|---|---|
| 납품 경로 | 출발 행, 도착 행 | 트랙(세로 주행 열) 1개 | 계단꼴 (직선 3개) |
| 반출 경로 | 상자 접점 행, 진출 변(N/S) | 트랙 1개 | 한꺾임꼴 (직선 2개) |

즉 **트랙 번호 하나만 정하면 경로 전체가 확정**된다. 탐색이 필요 없고,
같은 입력이면 항상 같은 그림이 나온다([[용어사전#결정성 (determinism)|결정성]]).

이 관점의 진짜 가치는 "빠르다"가 아니라 **충돌이 미리 보인다**는 것이다.
모든 경로가 몇 개의 숫자(행 2개 + 트랙 1개)로 요약되므로, 두 경로가 부딪히는지는
벨트를 깔기 전에 **숫자 비교만으로** 판정할 수 있다. 지금처럼 깔아 보고 나서야
"막혔네" 를 아는 구조와의 근본 차이가 여기다.

기존 코드에도 이 재료가 이미 있다: `channelPlanner.ts` 의
[[용어사전#left-edge algorithm|left-edge algorithm]](`assignTracksLeftEdge`)은 경로별 트랙 번호(`tracks` 배열)를
**이미 계산하고 있는데, 하류가 폭(`trackCount`)만 쓰고 트랙 번호는 버린다.** 이 버려지는 배열을
되살리는 것이 구현의 출발점이다(§8).

---

## 4. 같은 쪽 판정 — 반출 경로는 채널에 절단선을 긋는다

### 4.1 왜 갈리는가

반출 경로는 "벽의 한 점"에서 시작해 "열린 변(N/S)"에서 끝난다. 아래 그림에서
반출 경로(`╚═` + `║`)는 채널의 위쪽 끝까지 완전히 닿아 있다:

```
              N 변 (열린 끝)
              │ x1  x2  x3 │
  y=0         │  ②   ║  ①  │      ║ = 반출 경로의 세로 주행 (트랙 x2, N 으로 진출)
  y=1         │  ②   ║  ①  │      ① = 안쪽: 트랙 x2 의 동쪽 ∧ 진입 행보다 북쪽
  y=2         │  ②   ║  ①  │           (절단선에 둘러싸여 갇힌 구역)
  y=3 (진입행) │  .   ╚═════●      반출 대상 상자 (E벽)
  y=4         │  ②   ②   ② │      ② = 바깥쪽: 그 외 전부 (남쪽으로 서로 연결)
              S 변 (열린 끝)
```

지상 벨트는 서로 교차할 수 없으므로, 이 선은 채널을 **안쪽 ①과 바깥쪽 ②** 두 구역으로 가른다.
①에서 ②로 가려면 반드시 `║` 또는 `╚═` 를 건너야 하는데, 지상에서는 불가능하다.
— 이것이 절단선이다.

### 4.2 판정

> **납품 경로의 두 끝(출발 행 쪽, 도착 행 쪽)이 절단선의 같은 쪽이면 지상 계단꼴 경로가 존재한다.
> 다른 쪽이면(한쪽이 안쪽 ①에 갇히면) 지상 경로는 존재하지 않는다.**

판정에 필요한 것은 행·트랙 숫자 비교뿐이므로, **트랙 배정(패킹) 시점에 끝난다.**

### 4.3 Case A — 같은 쪽: 트랙만 잘 고르면 지상으로 해결

납품 경로의 출발 행 y=5(진입 행보다 남쪽), 도착 행 y=1. 둘 다 바깥쪽 ②.
세로 주행을 반출 경로의 **서쪽 트랙 x1** 에 배정하면 절단선을 건드리지 않는다:

```
              │ x1  x2  x3 │
  y=0         │  .   ║   .  │
  y=1 (도착행) ●═╪═╗   ║   .  │      부모 입력 상자
  y=2         │ ║   ║   .  │      납품 경로의 세로 주행(x1)이 반출 경로 서쪽으로만 지나감
  y=3 (진입행) │ ║   ╚══════●      반출 경로의 가로 진입은 x2~E벽만 점유 → 충돌 없음
  y=4         │ ║   .   .  │
  y=5 (출발행) │ ╚══════════●      자식 출력 상자
```

### 4.4 Case B — 다른 쪽: 지상 불가, 해소 사다리로

납품 경로의 출발 행 y=1 이 **안쪽 ①에 갇힌** 경우. 도착 행 y=2 는 바깥쪽 ②.

```
              │ x1  x2  x3 │
  y=0         │  .   ║   .  │
  y=1 (출발행) │  ✕   ║   ═══●     자식 출력 상자 (안쪽 ①)
  y=2 (도착행) ●═╪═✕   ║      │     부모 입력 상자 (바깥쪽 ②)
  y=3 (진입행) │  .   ╚══════●     건너려면 반드시 ║ 또는 ╚═ 를 넘어야 함 → 지상 불가
```

트랙을 어디로 골라도 소용없다 — 지상 경로 자체가 존재하지 않는다.
이때는 다음 순서로 해소한다 (전부 **계획 시점**의 결정이다):

1. **반출 경로 재배정** — 진출 변을 S 로 뒤집거나 트랙/상자 자리를 옮기면 안쪽 ① 구역이
   사라지거나 이동해서 갇힌 끝이 풀릴 수 있다. 계획기가 납품·반출 경로를 **한 장부에서**
   같이 보기 때문에 가능한 판단이다 (지금처럼 따로따로 깔면 불가능).
2. **지하 횡단** — 그래도 안 되면 해당 납품 경로가 절단선 밑을
   [[용어사전#지하벨트 (underground-belt)|지하벨트]]로 통과한다. 이것도 탐색이 아니라 좌표 배정이다.
3. **폴백** — 지하벨트가 선택 안 된 구성 등 1·2 가 모두 막히면 그 경로만 기존 dijkstra 로
   (실패 시 옛 경로 유지). 계획 단계에서 감지·로그되므로 조용한 회귀가 아니다.

---

## 5. 납품 경로끼리의 충돌 — 세 가지 경우와 "열 갈아타기"

같은 채널을 지나는 납품 경로 A(출발→도착 행 구간 a), B(구간 b)는 세로 구간의 겹침에 따라 셋으로 나뉜다:

1. **분리** (구간이 안 겹침) — 같은 트랙을 공유해도 된다. left-edge 가 이미 하는 일.
2. **부분 겹침, 순서 보존** (양 벽에서 위아래 순서가 같음) — 서로 다른 트랙에 두면 순수
   계단꼴끼리 성립한다(구현의 백트래킹 배정이 자동으로 찾는다). *(교정 2026-07-09: 설계
   초안은 이 경우도 모순이 생긴다고 봤으나, 구현 검증에서 트랙 분리만으로 풀림을 확인 —
   가로 진입/진출이 상대 트랙을 지나는 행이 상대의 세로 구간 밖이기 때문.)*
   **열 갈아타기(columnSwitch)** 가 필요한 경우는 더 제한적이다: 트랙들이 이미 다른
   경로 조각으로 파편화되어 **한 트랙이 구간 전체에서 비어 있지 않을 때**, 중간에 한 번
   꺾어 빈 조각 두 개를 이어 쓴다. 갈아타는 행은 충돌 없는 행에서 고른다 — 역시 좌표 배정:

```
              │ x1  x2  x3 │
  y=10        │  .   .  ╔══● A 출발
  y=19        │  ╔══════╝  │    ← A 가 y19 에서 서쪽으로 갈아탐 (x3 은 y19 아래가 막힘)
  y=20        │  ║   ╔═════● B 출발
  y=30  A도착 ●═╪══╝  ║     │    ← x1 경유, B 의 세로 주행(x2)을 안 건드림
  y=40  B도착 ●═╪═════╝     │    ← y40 은 A 구간(19..30) 밖 → x1 통과 OK
```

3. **완전 교차** (양 벽에서 끝 순서가 엇갈림 — A 가 E벽에선 위, W벽에선 아래) — 두 경로가
   벽에서 벽까지 이어지므로 **어떤 지상 모양으로도 반드시 교차한다**(열 갈아타기로도 불가).
   §4.4 의 사다리(지하 횡단→폴백)로 넘어간다. 단, [[용어사전#tidy-tree|tidy-tree]] 배치가
   형제 순서를 보존하므로 실제로는 드물다. *(대수로 쓴 판 — 순서 그래프의 순환 — 은 §10.2 정리 3.)*

---

## 6. 폭 역전 — 기하가 먼저, 폭은 결과

기존 순서는 "폭 계산 → 배치 → 라우팅" 이라 라우팅이 막혀도 폭을 되돌릴 수 없었다.
기하 예약에서는 순서가 뒤집힌다: **트랙·행 배정을 먼저 끝내고, 채널 폭은 그 결과(사용된 트랙 수)로부터 유도한다.**
배정 중 트랙이 모자라면 그 자리에서 폭을 늘리면 된다 — 벨트를 깔기 전이므로 아직 무엇도 확정되지 않았다.
dijkstra 를 유지하는 설계에서는 이 역전이 불가능하다(폭을 먼저 얼려야 탐색 공간이 정의되므로).

---

## 7. 대안 검토

| 대안 | 내용 | 기각 이유 |
|---|---|---|
| A. 교차 수만 세기 | `crossingCount` 로 폭만 여유 있게 | 폭이 넉넉해도 **배치**가 어긋나면 여전히 막힘 — 갇힘의 원인이 폭 부족이 아니라 조율 부재임이 실측으로 확인됨 |
| B. 라우터 강화 | dijkstra 우회·폴백 추가 | 실패를 늦출 뿐 보장이 없고, 결과가 입력에 민감해져 결정성·회귀 테스트가 약해짐. S-LAYER 의 "예약으로 실패 제거" 철학과 역행 |
| C. 채택안 | 예약을 폭→기하로 승격 | 충돌이 계획 시점에 전부 보이고, 해소도 계획 시점에 끝남. dijkstra 는 최후 폴백으로만 보존 |

---

## 8. 구현 위치 (2026-07-09 구현 완료)

**스위치는 없다.** 장부는 언제나 돈다(2026-09-08, off 모드 제거).

| 단계 | 파일 | 구현 |
|---|---|---|
| 통합 장부 | `channelGeometryPlanner.ts` (신규) | 납품·반출을 한 장부에서 배정. 같은 쪽 판정 → 해소 사다리(①진출 변 뒤집기 ②지하 횡단 — 세로/가로 두 변형 ③fallback 마킹). 지상 배정은 반복 심화 백트래킹(폭 최소 우선, 결정적), 실패 시 탐욕+열 갈아타기. fallback 경로도 폭은 phantom 트랙으로 예약 |
| 장부 호출·폭 역전 | `channel/ledger.ts`(planChannels) · `channel/shape.ts`(materializeChannelGeometry) · 적격성은 `link/arith.ts`(pairDeliveries) | 납품 경로 적격성(자식 출력 W변·부모 입력 E변) 분류 → 채널별 `planChannelGeometry`. **채널 폭 = 배정 결과 trackCount 에서 유도.** 배정을 절대좌표로 변환해 `PackResult.channelGeometry`(납품 경로 방출 지시 + 반출 예약 셀)로 방출. 부적격(스필 납품 경로)은 폭만 예약(`reserveIntervals`) |
| 납품 경로 좌표 방출 | `deliveryRoute.ts` | 계획 납품 경로는 계단꼴/열 갈아타기/지하 횡단을 **탐색 없이** 체인으로 방출(연속성 불변식 + 점유 검증, 어긋나면 dijkstra 폴백+로그). dijkstra 는 최후 폴백으로만 남고 **예약 셀(반출 lane + 다른 계획 납품 경로) 침범 금지** |
| 반출 경로 재생 | `perimeterExitPlanner.ts` / `perimeterRouter.ts` / `modulePerimeterPass.ts` | `ExitAssignment.entry`(진입 벽)·`trackX`(확정 트랙 x) 추가. ⑥C 는 `trackX` 를 스캔 없이 재생(막히면 기존 스캔 폴백). **직진** 반출도 예약 셀로 차단 — 옛 "straight blocked" skip 해소 |

미지원(폭만 예약, 기존 경로 유지): 스필로 채널을 정면으로 안 건너는 납품 경로(planner 면 개선이
근본 치료).

> **갱신(2026-07-11) — N/S 변 포트의 채널 우회 반출 해결.** 이 경로는 사실 장부가 이미
> 계획하고 있었다(코너 어깨 상자는 face 가 N/S여도 `meta.side`=E/W 라 exportRoute 로
> 정상 배정 + trackX·예약셀 생성). 유일한 구멍은 방출기가 `port.face` 로 진입 방향을 정하다
> fv.x=0 에서 거부한 것뿐 — trackX 로 구동하게 바꿔 elbow 를 그대로 재생한다. 예약된 채널
> 트랙이 상자 자신의 트렁크를 관통하는 드문 케이스(copper-cable)는 ⑥C 가 occ 기반 auto
> 탐색으로 폴백해 뚫린 face 로 내보낸다(dijkstra 최후폴백과 대칭). 상세: [.ns-face-relief §5](../module/ns-face-relief.md).

**코드 개명 대응표** (신규 코드는 문서 용어 사용, 기존 파일명 개명은 장기 과제):

| 문서 용어 | 현 코드 | 개명 목표 |
|---|---|---|
| 납품 경로 | delivery (`deliveryRoute.ts`) / 신규 `DeliveryInput`·`DeliveryPlan` | `deliveryRoute` |
| 반출 경로 | lane (`perimeterExitPlanner.ts`) / 신규 `ExportInput`·`ExportPlan` | `exportRoute` |
| 절단선 | (개념 — sameSideOfCut 내부) | `cut` / `cutLine` |
| 같은 쪽 판정 | `sameSideOfCut` ✓ | `sameSideOfCut` |
| 열 갈아타기 | `columnSwitch` ✓ (`tryColumnSwitch`) | `columnSwitch` |
| 지하 횡단 | `undergroundCrossing` ✓ (plan kind) | `undergroundCrossing` |
| 계단꼴 (납품 모양) | `staircase` ✓ (plan kind) | `staircase` |
| 한꺾임꼴 (반출 모양) | `elbow` ✓ (plan kind) | `elbow` |

---

## 9. 검증 계획 · 결과 (2026-07-09)

1. **골든 스냅샷** — `modulePipeline.golden.test.ts` 의 구현 전 스냅샷 대비, 의도된 diff 만 확인:
   **relocated 5→7, skipped 2→0, reason null** — 옛 skip 2건(kr-glass "no free track"— 당시 문자열은 "no free lane track",
   n0 출력 "straight blocked to W") 모두 전역 외곽 도달. hopFailures 0 유지.
2. **불변식 테스트** — `channelGeometryPlanner.test.ts` (15 케이스): (a) 납품 경로 belt 와 모듈
   셀(strip 제외)·다른 납품 경로의 셀 서로소, (b) 지상 계획 납품 경로는 corridor 0(판정-일치), (c) 결정성
   (같은 입력 → 같은 배정/같은 파이프라인 결과). 추가로 §4.3/§4.4(뒤집기·지하 횡단 두 변형)·
   §5-3 완전 교차 fallback 마킹·폭 역전을 단위 검증. 전체 스위트 212/212 green, tsc clean.
3. **실측** (2026-07-10, advanced-circuit 트리, COORD DUMP 좌표 검증) — 외부상자 7개 중
   6개가 전역 perimeter 도달: **kr-glass (16,−1) N 외곽 ✓**(원래 갇히던 상자), 루트 출력
   (−1,5) W 외곽 ✓, E 외곽 4개. 잔여 1건 = n0 copper-cable — SKIP 사유 "N/S-side channel
   divert unsupported"(N/S 변 포트의 채널 우회, §8 에 미지원으로 명시한 클래스 — 기존 동작과
   동일, 회귀 아님). deliveryRoute 폴백 로그 0, 콘솔 에러 0.
   > **갱신(2026-07-11):** 이 잔여 클래스 해결(§8 갱신 박스 참조). advanced-circuit 동형
   > 트리 count 1~8 단위 재현에서 상자 7개 전부 재배치, skip 0, 겹침 0 확인.

---

## 10. 배정의 정체 — **두 그래프(구간 · 순서)의 문제다**

> 2026-09-10 조사(옛 계획 `트랙-배정-개념정리`, 2026-09-12 여기로 옮겼다).
> §3 이 *"경로는 숫자 몇 개로 그려지는 도형"* 이라 했다 — 이 절은 그 숫자들 사이의 **충돌을
> 대수로 편다.** 그러면 이 장부가 왜 탐색이고 옆 장부(행 채널)는 왜 계산인지가 한 문장으로 갈린다.

### 10.1 충돌 규칙 다섯 — 도형에서 유도한다

`routeShape` 가 내는 경로 하나(납품 A):

```
A = (출발행 sA[E벽], 도착행 eA[W벽], 트랙 tA),  세로 구간 IA = [min(sA,eA), max(sA,eA)]

  가로 진입   행 sA,  열 [tA … capCol]     ← 내 트랙보다 **동쪽 전부**
  세로 주행   열 tA,  행 IA
  가로 진출   행 eA,  열 [-1 … tA]         ← 내 트랙보다 **서쪽 전부**
```

반출은 도착 끝이 바깥 변(`edge`)이라 가로 진출이 없고, 세로 주행이 변까지 뻗는다(진입 벽이
W 면 가로 진입이 서쪽 전부다). 출발행 = 도착행인 납품(`straight`)은 가로 하나가 `[-1 … capCol]`
을 **통째로** 덮는다.

`shapesConflict` 를 두 경로에 펴면 아래가 전부다(번호는 이 절 안에서만 쓴다):

| # | 무엇 ↔ 무엇 | 성립 조건 | 트랙이 나타나는 방식 | 부류 |
|---|---|---|---|---|
| 1 | 세로 ↔ 세로 | `tA = tB` ∧ `IA ∩ IB ≠ ∅` | 같음 | **무방향**(구간) |
| 2 | 가로 ↔ 가로, 같은 벽 | `sA = sB`(또는 `eA = eB`) | **안 나타남** | **절대**(트랙 무관) |
| 3 | 가로 ↔ 가로, 다른 벽 | `sA = eB` ∧ `tA ≤ tB` | 비교 | **방향**(순서) |
| 4 | A 가로 **진입** ↔ B 세로 | `tB ≥ tA` ∧ `sA ∈ IB` | 비교 | **방향**(순서) |
| 5 | A 가로 **진출** ↔ B 세로 | `tB ≤ tA` ∧ `eA ∈ IB` | 비교 | **방향**(순서) |

> **읽는 말:** 내 **출발 행**을 가로지르는 경로는 반드시 **내 서쪽**, 내 **도착 행**을
> 가로지르는 경로는 반드시 **내 동쪽**에 선다.

`straight` 는 규칙 2 의 극단이다 — **트랙을 0개 쓰면서 그 행을 통째로 잠근다.** 그 행을 지나는
모든 세로 주행이 트랙과 무관하게 막힌다. *"폭을 안 먹으니 공짜"* 가 아니다.

**여섯째 — 서로 다른 유체 쌍.** `conflicts` 는 여기에 **인접**을 더한다(두 유체가 닿으면 한
관망이 된다 — §8 의 유체 halo). 인접은 `|tA − tB| = 1` 같은 **거리** 조건이라 위 다섯과 성질이 다르다.

### 10.2 정리 셋

**정리 1 (순서 충족성) — 벨트끼리 · 같은 유체끼리에서.** 배정의 유효성은 트랙 번호의 **비교
결과**에만 달렸다. 규칙 1~5 에서 `t` 가 나타나는 자리가 전부 `=` · `≤` · `≥` 이고, 벽(`-1` ·
`capCol`)은 언제나 모든 트랙의 바깥이라 비교를 안 바꾼다.

- **따름 — 압축이 안전하다.** 유효한 배정에서 쓴 트랙 값을 순서를 지킨 채 `0,1,2,…` 로 눌러도
  유효하다 → 최소 트랙 수 = **필요한 서로 다른 계급의 수**.
- **다만 서로 다른 유체가 둘 이상이면 성립하지 않는다** — 인접 조건(거리)이 끼어, 순서를 지켜
  눌러도 떨어져 있던 두 유체가 붙을 수 있다.
- **값 대칭은 없다.** 트랙 번호는 이름표가 아니라 **서·동 좌표**다(규칙 4·5 가 그 값을 비교한다).
  그래서 *"첫 경로를 `t=0` 에 못박는"* 대칭 깨기는 **해를 지운다**:
  `A: 10→30`, `B: 8→12` 면 B 의 구간이 A 의 출발행 10 을 덮어 `tB < tA` 가 필수인데, A 를 0 에
  박으면 `tB < 0` 이 되어 해가 있는데 없다고 답한다.

**정리 2 (하한).** 세로 구간이 겹치는 둘은 같은 트랙에 못 온다(규칙 1) → **트랙 수 ≥ 세로 구간의
최대 동시 겹침 수(density)**. 그 수는 `assignTracksLeftEdge(구간들).trackCount` 가 이미 낸다.

**정리 3 (불가능).** 규칙 3·4·5 를 방향 간선으로 모은 **순서 그래프에 순환이 있으면**, 트랙을
몇 개 주어도 지상 해가 없다. 폭을 넓혀서는 못 푼다.

```
반례(순환):  A: 10 → 30      B: 20 → 5
             B 의 세로가 A 의 출발행 10 을 지난다  →  규칙 4:  tB < tA
             A 의 세로가 B 의 출발행 20 을 지난다  →  규칙 4:  tA < tB    모순
```

이것이 §5-3 의 *"완전 교차"* 이고 코드 주석의 조르당 곡선 논증의 대수판이다. 그때 필요한 것은
폭이 아니라 **차원**이다 — 지하 횡단(층 바꾸기) · 열 갈아타기(dogleg).

**정식 이름은 VLSI 채널 라우팅이다.** 규칙 1 = 수평 제약(구간 그래프), 규칙 3·4·5 = 수직 제약
그래프(VCG). 알려진 것: VCG 가 없으면 left-edge 가 최적 · VCG 가 비순환이면 해는 있으나 최소 폭은
일반형이 NP-완전(Szymanski 1985) · VCG 가 순환이면 dogleg·층 바꾸기 없이 해가 없다.

### 10.3 같은 축의 배정기 — **한쪽 끝이 공통이면 계산, 양 끝이 자유로우면 탐색**

| 배정기 | 구간(규칙 1) | 순서(규칙 3·4·5) | 오늘 푸는 법 | 왜 그것으로 되나 |
|---|---|---|---|---|
| `channelPlanner.assignTracksLeftEdge` | ✅ | 없다 | **계산** — 정렬 first-fit, 최적 | 구간 그래프뿐이다 |
| `rowChannelPlanner.planRowChannel` | ✅ | ✅ **전순서로 모인다** | **계산** — 정렬 두 번 | 통과 경로의 **서쪽 끝이 공통**이라 부등식이 한 방향으로 모인다 |
| `channelGeometryPlanner`(이 문서) | ✅ | ✅ **순환이 날 수 있다** | **탐색** — 반복 심화 백트래킹 + 사다리 | 양 끝이 다 포트에 묶여 순서가 안 모인다 |

**세로 채널이 탐색인 것은 알고리즘 취향이 아니라 입력의 모양 때문이다.** 그래서 *"세로 채널도
계산으로"* 는 입력을 바꾸지 않는 한 불가능하다 — 바꿀 수 있는 것은 탐색이 **도는 범위**와
**공간의 차원**뿐이다(§10.4). 행 채널의 계산도 *"서쪽 끝 공통"* 하나에 기대므로, 통과 경로가
서쪽 변까지 안 가는 날(예: 동쪽으로 나가는 반출이 행 채널에 신고하는 날) 순환이 생길 수 있다.

> 모듈 안쪽의 결정적 배정 2·3단([[trunk-assignment]] 구현 상태 표 — 반복 심화 백트래킹 ·
> 탐욕 폴백)도 같은 뼈대다. 거기서 먼저 물을 것도 같다 — **깊이 번호는 이름표인가 좌표인가.**

### 10.4 오늘 탐색기의 알려진 비용 — **답은 옳고, 값이 비싸거나 사유가 거칠다**

넷 다 **배정을 틀리게 하지는 않는다**(불변식이 테스트로 잠겨 있다). *"같은 답을 더 싸게, 실패를
더 정확하게"* 의 방향이고, 착수 조건은 두지 않는다(실측 수치는 착수 조건이 아니다 —
`tempPlanDocs/CLAUDE.md`). 싸고 위험이 낮은 것부터 적는다.

| | 코드의 성질 | 개선 방향 |
|---|---|---|
| **T-C** | 순환을 **먼저** 안 본다 — 순환이면 전 라운드가 실패로 정해져 있는데 끝까지 돌고, 사유는 `no-surface-assignment` 한 마디다(`flg.report()` 의 `납품장부` 줄) | 규칙 3·4·5 로 방향 그래프를 세워 순환을 검출한다(판정만 는다). 사유가 *폭 부족* 과 *순환* 으로 갈린다 |
| **T-A** | `T` 를 **1 부터** 올린다 — `T < density` 인 라운드는 정리 2 로 **반드시 실패**하는데, 그 실패의 증명이 전 탐색이다 | `T` 를 density 에서 시작한다. 구간 집합은 탐색 항목과 같아야 한다(`straight` · 물러난 반출 · 절단선에 갇힌 납품 제외) |
| **T-D1** | 예산(`SEARCH_BUDGET`)이 **라운드 사이에 공유**된다 — `T` 루프 밖에서 한 번 만들어져, 앞 라운드가 태우면 해가 있는 뒤 라운드를 못 보고 탐욕으로 내려간다 | T-A 와 한 몸이다 — 태워지는 라운드가 대개 `T < density` 다 |
| **T-D2** | 탐욕 폴백의 순서가 **실패 비용 순**(유체 → 반출 → 아이템, 각각 id 순)이다 — left-edge 의 최적성은 구간 시작(lo) 순에서 나오므로 폭이 필요 이상 넓을 수 있다 | **버그가 아니라 교환이다**(유체 우선이 최적성보다 중요하다). 우선순위 무리 **안에서만** lo 로 정렬하면 둘 다 지킨다 |
| **T-B** | 탐색 공간이 `T^n` 이다 — 유효성이 순서에만 달렸는데(정리 1) 같은 순서의 배정을 여러 번 방문한다 | 약순서 공간을 훑는다. **탐색기 자체를 바꾸는 일**이라 가장 비싸고, 결정성을 새로 세워야 한다. 앞의 넷이 서면 탐색이 도는 범위가 좁아져 값이 줄어 있을 수 있다 |
