---
tags: [auto-layout, placement, routing]
---

> **관련 문서:** [[code-folders]] — 코드 폴더의 경계(이 문서는 **모델**의 경계다) ·
> [[channel-geometry-reservation]] — 채널 모델의 본문 ·
> [[module-planning]] — 모듈 모델의 본문 · [[perimeter-export]] — 반출 모델의 본문

# 배치 모델 — 각자 무엇을 그릴 수 있나 (넷 + 빈자리 하나)

## 0. 왜 이 문서인가

자동 배치는 **모델 넷**이 이어 붙어 돈다(그리고 **빈자리가 하나** 있다 — §2⑤). 각 모델은 자기 좌표계에서 **정해진 도형만** 그린다 —
탐색이 아니라 배정이기 때문이다(예약 철학).

문제는 **그릴 수 없는 것**이 어디에도 안 적혀 있다는 것이다. 코드에는 그 경계가
*"판정"* 이나 *"조건"* 처럼 생긴 이름으로 들어 있어(`eligible`), **넓히면 되는 문턱**으로 보인다.
실제로는 **모델이 표현할 수 있는 범위**라서, 넓히면 못 그리는 그림이 나온다.

> **2026-08-18 실측:** `eligible` 을 넓혔더니 계단꼴이 모듈 몸통을 관통하는 경로를 그렸고,
> 계획된 납품 경로가 **0개**가 됐다. 게이트를 연 것이 아니라 **모델 밖을 그리게 한 것**이다.

이 문서는 두 축을 적는다:

```
§2    모델마다 **좌표계 · 고정/자유 · 그릴 수 있는 도형 · 경계**   ← 무엇을 그릴 수 있나
§3.5  경로 넷 · 장부 여섯 · **순서(우선권)**                      ← 누가 공간을 언제 먹나
```

**둘 다 있어야 증상을 읽는다.** 도형이 맞아도 자리가 없으면 못 그리고, 자리가 있어도
도형이 없으면 못 그린다 — §4 의 표가 증상을 그 둘로 가른다.

---

## 1. 한눈에

```
① 모듈 모델      한 모듈 안쪽.  기둥 + 면 + 깊이×행
② 트리 배치 모델  모듈들의 자리.  depth = 열 · tidy-tree = 행
③ 채널 모델      모듈 **사이 세로** 통로([[용어사전#채널 (channel)|채널]]).  트랙 + 계단꼴/한꺾임꼴
④ 반출 모델      갇힌 상자 → 배치 **밖** 테두리

⑤ **행 채널 모델 — 이름만 있고 없다**
   [[용어사전#행 채널 (row channel)|행 채널]] = 세로로 쌓인 두 모듈 **사이의 가로 통로**.
   채널을 90° 돌린 **직교 짝**으로 용어사전에 정의돼 있으나, **계획 모델이 없다.**
```

**셋(①③④)이 경로를 그리고, ②가 그 무대를 만든다.** 그리고 넷은 서로를 안 본다 —
사이를 잇는 것은 **자료형 하나**씩이다(§3).

> **⑤는 2026-09-06 에 빈자리를 벗어났다.** 용어사전 `행 채널` 항목이
> *"모듈 간 납품 경로 벨트와 외부상자 반출 트랙이 여기를 지난다 —
> 납품 경로가 여기를 가로막으면 상자가 perimeter 로 못 빠져나가는 **skip 의 무대**"*
> 라고 역할과 사고를 미리 적어 놨고, 이제 그것을 **계획하는 코드가 있다.**
> 높이는 통과 경로 수에서 유도되고(`planRowChannel`), 그 높이가 곧 **모듈 사이 간격**이다
> — 옛 `STACK_GAP = 3` 상수가 있던 자리다.

---

## 2. 모델마다

### ① 모듈 모델 — 기둥 하나의 면

| | |
|---|---|
| **좌표계** | 모듈-로컬. 면(W/E/N/S) × 깊이 `d`(면에서 바깥으로) × 위치 `t`(면을 따라). **면마다 `FaceTable` 한 장** — 칸 = `(행, 깊이)`([[module-planning]] §4.5) |
| **고정** | 머신 대수·크기(기둥은 세로 한 줄), 유체 면(머신 `fluid_boxes` 가 정한다) |
| **자유** | 줄마다: 어느 면 · 어느 깊이(깊이) · 어느 좌석 행 |
| **도형** | 깊이 하나 = 세로 벨트 한 줄 + 그 좌석들. 포트는 벨트 끝 + 인서터 + 상자 |

**자원이 둘이다:** 좌석 행(머신마다 `h` 개)과 **깊이 × 행**. 깊이 수는 팔 종류(reach) 수에서
나온다 — reach `r` 인 팔은 `d(r+1)` 을 집는다.

**둘은 한 표의 두 열이다**(2026-08-26). `d1` 열이 좌석, `d2..` 열이 깊이가고, **포트 인서터·
상자도 그 표의 칸**이다 — 예전엔 벨트 깊이만 장부에 실려서 포트가 남의 깊이에 서는 것을
아무도 몰랐다. 그리고 **깊이를 고르면 팔 종류가 정해지므로 팔 *개수*가 깊이마다 다르다**
(`armsAt`) — 좌석 예산은 깊이 후보마다 다시 재야 한다.

```
관통(g = N)   벨트가 기둥 전체 → 깊이를 통째로 먹는다 → 포트가 **기둥 끝**
구간(g < N)   벨트가 일부     → 행만 먹는다          → 포트가 **면 바깥**
```

**그릴 수 없는 것**

```
· 격자(2D) 클러스터 — 기둥은 세로 한 줄로 못 박혀 있다 (`clusterLayout`)
· 한 벨트가 비연속 구간 — 벨트는 연속이라야 흐른다
· 면에 관통이 깊이 수보다 많은 것 — 그 이상은 다른 면이나 gap 으로 간다
```

### ② 트리 배치 모델 — 무대를 만든다

| | |
|---|---|
| **좌표계** | 절대. `colX[depth]` · `topY[node]` |
| **고정** | 트리 구조(`parentId` 는 **스칼라**), 각 모듈의 extent |
| **자유** | 같은 깊이 안에서의 세로 순서·간격 |
| **도형** | 열(깊이) + 세로 스택. 열 사이가 채널, 스택 사이가 **행 채널** |

```
colX[d] = colX[d-1] + colWidth[d-1] + channelWidth(d)
colWidth[d] = max(그 깊이 모듈들의 extent.w)
```

**그릴 수 없는 것**

```
· DAG — 자식이 부모를 여럿 가질 수 없다.  중앙 정렬(`tree/shape.stackColumns`)이 트리를 전제한다
```

### ③ 채널 모델 — 모듈 사이 세로 통로

| | |
|---|---|
| **좌표계** | **반쪽 추상**. 열 = `-1`(W벽) · `0..cap-1`(트랙) · `capCol`(E벽) — **가상**<br>행 = **절대 y** — 실좌표 |
| **고정** | 납품의 출발 행·도착 행, 반출의 접점 행 (**포트가 정한다**) |
| **자유** | 트랙(세로 주행 열) 하나 |
| **도형** | 납품 = 계단꼴(직선 3) · 반출 = 한꺾임꼴(직선 2) · 열 갈아타기 · 지하 횡단 |

**핵심 도구는 절단선(cut)** — 반출이 채널을 두 쪽으로 가르고, 납품의 두 끝이 같은 쪽이라야
지상 경로가 존재한다. 벨트를 깔기 전에 **숫자 비교만으로** 판정된다.

**그릴 수 없는 것 — 그리고 그 경계의 이름이 `eligible` 이다**

```
전제   포트가 **채널 벽을 마주 본다**  (자식 출력 W변 · 부모 입력 E변 · 깊이 인접)
표현   link/arith.ts pairDeliveries  `eligible = out.meta.side === "W" && inp.meta.side === "E" && …`

그래야 "출발 행 · 도착 행" 이 뜻을 갖는다 — 벨트가 벽에서 **가로로** 나와 채널로 든다.
포트가 기둥 끝(N/S)이면 벨트가 **세로로** 나오므로 "출발 행" 이 아니라 "출발 열" 이고,
계단꼴은 그 모양을 표현할 수 없다.
```

```
· 열이 가상이라 **모듈 내부를 모른다** — 벽 안쪽은 좌표계 밖이다
· 여러 채널을 가로지르는 경로 — 좌표계가 채널 하나 안이다
· 기둥 끝 ↔ 기둥 끝 납품 — 열 채널이 아니라 행 채널을 지나야 하는데 그 개념이 없다
```

### ④ 반출 모델 — 배치 밖으로

| | |
|---|---|
| **좌표계** | 절대 |
| **고정** | 상자 자리, 그 포트의 `moduleWayOuts`(모듈이 자기에 대해 답한다) |
| **자유** | 진출 변(N/S/W/E), 채널 host 면 트랙 |
| **도형** | 직진 · 한꺾임꼴(채널 경유) |

**탐색 폴백이 없다** — *"예약된 경로는 항상 방출 가능해야 한다"* 가 설계다. 못 놓으면
그 상자만 로컬 ring 에 남기고 **사유를 남긴다**(`skips`).

### ⑤ 행 채널 모델 — ③의 **전치**

| | |
|---|---|
| **좌표계** | 행 채널 안의 **행 번호**(트랙 index). 절대 행은 `rowChannel.top + track` |
| **고정** | 행 채널의 **신원**(깊이 · 순번 · 이웃) — 트리 DFS 순서가 정한다 |
| **자유** | 어느 트랙에 앉나 |
| **도형** | 가로 직선. 포트에서 그 행까지 내려오는 **세로 진입**은 아직 배정 밖이다 |

**높이는 고르는 값이 아니라 배정의 결과다** — `channelWidthFromTracks(trackCount, 3)`,
세로 채널과 **같은 식**이다. 그리고 그 높이가 **모듈 사이 간격을 민다**(`tree/shape.stackColumns` 의 누적합 · 하한 복원)
— 그래서 행 채널이 트랙보다 좁을 수 없다.

> **⑤가 서면 "주체 둘" 문제가 따라온다**(2026-08-18). 세로 채널 장부와 행 채널 장부가
> 각자 예약하면 2026-08-17 의 사고(*"남의 계획을 밟는다"*)가 축만 바꿔 재현된다.
> 그래서 **통로망 모델**(채널 + 행 채널 + 교차로를 한 주체가 배정)이 후보로 있다 —
> 그러면 ③은 *"한 세로 통로 안의 배정"* 을 맡는 **부품**이 된다.
> → `tempPlanDocs/채널-양보-명세/judgements.md` `J-관리관`

**있어야 할 것은 ③의 전치(轉置)다.** `assignTracksLeftEdge`(구간 겹침 → 최소 트랙)는
**축과 무관한 순수 알고리즘**이라 그대로 쓸 수 있다. 그 위의 것(계단꼴·절단선·같은 쪽
판정)은 전부 세로 전용이라 다시 세워야 한다.

**왜 이 자리가 필요한가**

```
기둥 끝 포트끼리의 납품   상자가 기둥 위/아래에 있다 → 열 채널이 아니라 **행 채널**을 지난다
반출 반출 트랙            갇힌 상자가 N/S 변으로 나가려면 이 행 채널을 지난다
사용자 요구               "블루프린트를 거의 관통하는 가로 벨트"
```


---

## 3. 모델 사이의 접점 — 각자 **자료형 하나**

```
①  →  ③④    ModulePort   { anchor(=상자=모듈 경계) · tapAnchor(=벨트 끝) · face · moduleWayOuts }
②  →  ③     DeliverySpec + 절대 포트 y
②  →  ④     PerimeterExitPlan
③  →  방출  DeliveryGeometry (트랙 번호 → 호출자가 절대 x 로)
```

**접점이 얇은 것이 설계다.** 모듈은 자기 안을 안 보여 주고(불투명 블록), 채널은 모듈 안을
모른다. 그래서 한쪽을 바꿔도 다른 쪽이 안 깨진다 — **접점의 뜻이 그대로인 한**.

> **`ModulePort.anchor` 의 정의는 "모듈 경계"다**(`clusterModule.ts`). 실제로는
> *"자기 벨트 + 2"* 에 놓이므로, 그 둘이 같아지는 것은 **그 면에 이 그룹 혼자일 때**뿐이다.
> 면에 여러 줄이 앉으면 얕은 줄의 상자가 깊은 줄에 둘러싸여 **경계가 아니게 된다.**

---

## 3.5 자원과 우선권 — **누가 공간을 언제 먹나**

§2 가 *"각 모델이 무엇을 그릴 수 있나"*(도형)라면, 이 절은 **자원**이다. 도형이 맞아도
자리가 없으면 못 그리고, 자리는 **먼저 오는 쪽**이 가진다.

> **이 절이 없어서 2026-08-18 에 오래 헤맸다.** *"마진"*·*"통로"* 같은 이름부터 쓰다가
> 매번 정정했다. 이름은 이 표 위에 얹힌 것이고, **바닥은 경로와 순서**다.

### 경로는 넷

| | 무엇 | 어디서 어디로 | 셀을 만드는 곳 |
|---|---|---|---|
| **A. 모듈 안** | 트렁크 벨트 · 좌석 인서터 · 포트 끝 | 머신 ↔ 자기 포트 상자 | `emitModule` |
| **B. 납품** | 자식 출력 → 부모 입력 | 모듈 ↔ 모듈 | `deliveryRoute` |
| **C. 반출** | 갇힌 상자 → 배치 밖 테두리 | 모듈 → [[용어사전#perimeter ring\|perimeter ring]] | `modulePerimeterPass` |
| **D. 유체** | 트렁크 파이프 · [ClusterPipe] | A·B 의 유체판 | `emitModule` · `emitPath` |

### 공간은 **하나**다 — 격자 전체

그 하나를 **여섯 장부**가 나눠서 본다. 장부가 여럿인 것이 문제가 아니라,
**서로를 모르는 것**이 문제다.

```
① clusterModule.occupancy        모듈 하나 안.  머신 + 자기 셀
② modulePacking.reservedExport   C 가 ring 까지 갈 직선 (셀 단위 예약)
③ channelGeometryPlanner.occ     세로 채널 안의 추상 셀 (열, 행)
④ deliveryRoute.buildOccupancy   **전 모듈**의 셀 (A 가 다 놓인 뒤)
⑤ deliveryRoute.deliveryBelts    이미 깐 B
⑥ modulePerimeterPass.occ        ④ + B 전부
```

### 순서가 곧 우선권이다

```
A 가 먼저 놓는다        모듈 생성 — 형제를 모른다
  ↓  ④가 그걸 읽는다
C 가 자리를 **예약**한다  ② — ring 까지의 직선을 셀로 잠근다
  ↓
B 가 계획한다            ③ 에서 배정 → ④⑤② 로 검사 → 막히면 버리고 dijkstra
  ↓
C 가 실제로 놓는다        ⑥ 으로 확인 → 막히면 skip
```

**뒤에 오는 쪽은 앞의 자리를 못 바꾼다.**

단 **한 곳만 예외다** — `channelGeometryPlanner` 안에서는 B 와 C 가 *동시에* 배정된다
(아래 참조). 채널 **밖**의 반출(`exitMode` 가 **직진**)은 그 협상에 아예 안 들어가고
곧장 ②에 직선으로 박힌다. 거기서는 아무도 *"내가 먹으면 남이 곤란해지나"* 를 안 본다.

```
B 가 막히면   → dijkstra 폴백 (남의 예약을 밟는다.  연쇄한다)
C 가 막히면   → skip (상자가 갇힌다)
A 는          → 애초에 형제를 모른다 (설계상 옳다)
```

### 그래서 증상이 이렇게 읽힌다

| 증상 | 무엇을 뜻하나 |
|---|---|
| `chain-blocked: 모듈 몸통` | B 의 경로가 A 의 자리를 지난다. **도형 문제**(§2) |
| `chain-blocked: 반출 예약` | C 가 B 와 **협상 없이** 자리를 박았다. **양보 문제**(이 절) |
| `reservation not emittable` | C 가 예약할 땐 비었는데 놓을 땐 막혔다. 대개 B 의 dijkstra 폴백이 밟았다 |
| `예약 무시 재시도` | B 가 폴백조차 막혀 **남의 예약을 의도적으로 밟았다**. 연쇄의 시작점 |

### 순서만으로는 설명이 안 된다 — **채널 안은 이미 공동 배정이다**

> 이 절의 초판(2026-08-18)은 *"B 가 가장 나중이라 밀린다 → 순서를 A → B → C 로 뒤집자"* 로
> 끝났다. **틀렸다.** 다음 날 코드를 읽어 보니 채널 안에는 그 순서가 없었다.

`channelGeometryPlanner` 는 B(납품)와 C(반출)를 **함께** 배정한다:

```
① 절단선 라운드   유체 납품 vs 반출 → 안 풀리면 **반출이 물러난다**(yieldedExports)
                  아이템 납품 vs 반출 → 안 풀리면 그 납품이 지하로(cutOff)
② 지상 배정       유체 → 반출 → 아이템 순으로 iterative-deepening 백트래킹
```

즉 **채널 안에는 이미 양보도 있고 제약우선도 있다**(실패 비용이 큰 유체가 앞). 순서를
뒤집어도 여기선 아무 일도 안 일어난다.

밀리는 것은 채널 **밖**이다 — `exitMode` 가 **직진**(`direct`)인 반출.
이들은 **공동 탐색을 안 거치고** 곧장 ②`reservedExportCells` 에 직선으로 박힌다.
B 는 협상 상대를 만난 적이 없다.

### 진짜 축은 순서가 아니라 **양보**다

`perimeter/types.ExitAssignment.options: ExitOption[]` — **자유도가 이미 자료형으로 있다.**
그 주석이 의도까지 적어 뒀다:

> 출구 선택은 자유도다. 더 까다로운 제약을 가진 장부가 **양보를 요구**할 수 있으므로,
> planner 가 하나로 못박지 않고 후보를 남겨 장부가 고르게 한다(**스도쿠: 제약 센 곳부터**).

**그런데 `options` 를 읽는 코드가 0곳이다.** `options[0]` 이 확정으로 박히고 아무도 뒤 후보를
묻지 않는다. 자유는 선언돼 있고 **한 번도 쓰인 적이 없다**.

```
순서를 뒤집는다  →  채널 안: 효과 0.  채널 밖: 이번엔 C 가 밀린다 — 문제를 옮길 뿐
양보를 켠다      →  자유도가 큰 쪽이 작은 쪽에 길을 내준다.  순서와 무관하게 성립
```

### 경쟁은 **좌표계가 만든다**

로컬 좌표로 자기를 표현하는 주체는 **자기 안에서 배정을 끝내고** 절대좌표로 나온다 — 나올
때는 이미 정합적인 덩어리다. 절대 좌표로 **바로 쓰는** 주체는 전역 격자에서 만나 경쟁한다.

| 주체 | 좌표계 | 절대화 지점 | 결과 |
|---|---|---|---|
| **A** 모듈 안 | 모듈 로컬 | `moduleTransform`(packing 때) | 로컬 동안 경쟁 불가. 나온 뒤엔 **남이 읽기만** 한다 |
| **③** 세로 채널 | 채널 로컬(트랙 index) | `channelStartX(d)+1+track` | **로컬 안에서 B·C 를 공동 배정** ← 경쟁이 풀리는 유일한 곳 |
| **⑤** 행 채널 | 행 채널 로컬(`band.top+t`) | `bandApproach.row` | 로컬 배정은 하는데 **결과를 아무 장부에도 안 싣는다** |
| **B** 납품 | **처음부터 절대** | — | 전역에서 경쟁 |
| **C** 반출 | **처음부터 절대** | — | 전역에서 경쟁 |

**③ 이 경쟁을 푸는 이유는 똑똑해서가 아니라 로컬 좌표를 가져서다.** 로컬 좌표는 *"내 안의
자리는 내가 전부 안다"* 는 뜻이고, 그래야 공동 배정이 성립한다. ⑤ 는 좌표계는 가졌는데
**절대화한 결과를 예약 안 해서** 반쪽이다.

### 셀 경쟁 전수표

`○` = 판정 있음(옳다) · `△` = 판정은 있으나 방향이 어긋남 · `✗` = 판정 없음

| # | 다투는 쌍 | 지금 누가 이기나 | 근거(코드) | 상태 |
|---|---|---|---|---|
| 1 | 머신 footprint ↔ 전부 | **머신** | `buildOccupancy` · `occ` | ○ |
| 2 | A ↔ A (같은 모듈) | **배정이 판정한다** | `FaceTable`(계획) → `clusterModule.occupancy`(확인) | △ **기둥 밖은 예외**(아래) |
| 3 | A ↔ A (다른 모듈) | 누적합이 **벌려 놓는다** | `tree/shape.stackColumns`(누적합 · 하한 복원), **모듈 bbox 단위** | ○ 행 채널 높이가 완충 |
| 4 | A ↔ B | **A** | `deliveryRoute.base` | ○ |
| 5 | A ↔ C | **A** | `perimeterPass.occ` | ○ |
| 6 | B ↔ B (아이템) | 먼저 깐 쪽 + 예약 상호검사 | `deliveryBelts` · `reservedDelivery` | ○ |
| 7 | **B유체 ↔ B유체(다른 유체)** | 먼저 깐 쪽 — **겹침만** | `blocked ⊇ deliveryBelts` | **✗ 인접 미검사** |
| 8 | B ↔ C **예약** | **C** (B 가 사유 내고 물러남) | `reservedExport` | △ |
| 9 | B ↔ C **방출** | **B** (C 가 skip) | `occ ⊇ deliveryCells` | △ **8과 방향이 반대** |
| 10 | C ↔ C | 먼저 나간 쪽(id 순) | `occ` 증분 | ○ |
| 11 | A유체 ↔ B유체 | **A** — 인접까지 | `fluidBlocked` | ○ |
| 12 | A유체 ↔ C파이프 | **A** — 인접까지 | `pipeFlowByFluid` | ○ |
| 13 | **B유체 ↔ C파이프** | 겹침만 | `pipeFlowByFluid` 가 **납품 전에 얼어** B 를 모른다 | **✗ 인접 미검사** |
| 14 | ③ 안의 B ↔ C | **공동 배정** — 유체가 갇히면 반출이 물러난다 | 백트래킹 + `yieldedExports` | ○ **여기만 풀렸다** |
| 15 | **⑤ 행 채널 ↔ C 예약** | C — **가로질러도 아무도 모른다** | 행 채널에 예약이 없다 · 특히 **진입 다리**는 계획에 자료조차 없다(`구간-밖-주행` 트랙 B) | **✗** |
| 16 | **마진 프레임 ↔ B** | B — bounds 가 마진을 포함 | 직선(`addSeg`)만 예약 | **✗** |
| 17 | ⑤ 행 채널 ↔ A | 안 겹친다 | 행 채널 높이가 **곧 그 간격이다** | ○ |

> **#2 의 예외 — 기둥 **밖**은 배정이 못 본다**(2026-09-06 확인).
> `FaceTable` 의 행은 `0 … rowsPerMachine×머신수−1` 뿐이라 **기둥 끝 포트가 기둥 밖에 내는
> 칸들**(관통 줄은 팔·상자 둘, 레인 합류는 합류 칸까지 셋)이 표에 없다 — `portCells` 의
> 마지막 `filter` 가 그 행들을 버린다. 그렇다고 **띠도 아니다**: `moduleExtent` 가 모든 셀을
> 감싸므로 그 사슬은 모듈 extent **안**이고 띠는 extent 밖에서 시작한다.
>
> 오늘 그 자리를 막는 것은 `ctx.ends` 하나뿐이고 낟알이 **면당 N·S 둘**이다. 그래서
> 깊이가 달라 안 부딪히는 두 관통 줄도 못 나눠 쓰고(*"세 번째 관통은 옆으로 물러난다"*),
> 그 거친 예약을 떼는 순간 도형끼리 겹친다(2026-09-05 실측: 합류 쌍 둘이 같은 띠에서 충돌).
> → `tempPlanDocs/구간-밖-주행/` **트랙 A**
>
> (2026-08-26 이전엔 기둥 **안**의 포트 칸도 장부 밖이라 방출에서야 갈렸다. 그건 닫혔다.)

**8·9 가 이 표의 핵심이다.** C 는 **예약으로 이기고 방출로 진다** — 그 사이에 B 가 끼어 있고,
C 는 자기 예약이 아직 유효한지 다시 안 본다. `reservation not emittable` 이 그 자국이다.

### 우선권의 근거는 **실패했을 때의 피해**다

```
① 머신이 굶는다        트렁크 끊김 · seat 덮임          §0.1 목적 직접 위반  ── 치명
② 유체가 오염된다       다른 유체와 인접 합류            화면상 멀쩡해서 더 나쁘다 ── 치명
③ 납품이 안 이어진다    B 실패 → 부모 머신이 굶는다      ①로 귀결 ── 치명
④ 반출이 못 나간다      C skip → 상자가 모듈 안에 남음   **물류는 이어진다** ── 경고
⑤ 폭이 낭비된다        안 쓸 트랙을 예약                 ── 미관
```

**이 등급이 곧 우선권이다. 그리고 규칙은 이미 코드에 있다** — ③의 *"유체가 갇히면 반출이
물러난다"* (`yieldedExports`)가 ②>④ 를 옮긴 것이다. **채널 안에서만 쓰인다**(#14).
#8·#15·#16 은 같은 규칙을 채널 밖에 안 쓴 자리다.

### 유도되는 것 vs 사용자가 정할 것

§0.1(*목적 하나 — 모든 머신 100% 가동*)에서 **유도된다** — 물을 필요가 없다:

```
머신 > 전부          |  유체 > 아이템 (실패가 치명적이고 조용하다)
납품(B) > 반출(C)     |  자유도가 작은 쪽이 먼저
```

목적이 이미 충족된 뒤 **비용만 다른** 것 — 여기서만 사용자가 정한다:

| | 물음 | 무엇과 무엇의 교환 |
|---|---|---|
| **가** | C 가 못 나갈 때 상자를 안에 남기나, 폭을 넓혀 자리를 만드나 | 면적 ↔ 완결성 |
| **나** | `PERIMETER_MARGIN = 2` — 반출 전용 테두리를 몇 칸 줄까 | 면적 ↔ 반출 성공률 |
| ~~다~~ | ~~`STACK_GAP` 을 수요에서 유도할까~~ — **유도한다**(2026-09-06). 3 은 하한으로 남았다 | ~~면적 ↔ 납품 성공률~~ |
| **라** | B 가 마진(#16)을 써도 되나 | 경로 길이 ↔ C 의 안전 |

**이미 정해진 것:** 지하 사거리가 짧으면 **파이프 배치를 우선**한다(2026-08-18 사용자).

### 고치기 전에 재는 법 — 자유도는 **한 배정 순간의 성질**이다

*"C 가 B 보다 자유롭다"* 는 측정할 수 없는 문장이다. 물을 수 있는 것은 하나뿐:

> **실제로 막은 그 셀을 만든 배정이, 그때 다른 선택지를 가지고 있었는가.**
> 단위 = `options.length`, 그리고 *그중 상대를 안 막는 것의 수*.

| 나오는 값 | 뜻 |
|---|---|
| `options.length == 1` | 자유가 없었다 — 자리를 **늘리는** 쪽(마진·행 채널·폭)이 답 |
| `> 1`, 대안이 전부 막음 | 쓸모없는 자유 |
| `> 1`, 안 막는 대안 있음 | **양보가 있었으면 풀렸다** — 협상의 부재 |

**계측의 전제:** 지금 사유는 `반출 예약 (8,15)` 로 **칸만** 말하고 주인을 안 말한다
(`deliveryRoute.plannedChainClear`). 주인을 모르면 위 표를 물을 수 없다 —
`reservedExportCells` 를 `Set<셀>` → `Map<셀, 배정id>` 로 바꾸는 것이 계측기다.
→ `tempPlanDocs/채널-양보-명세/judgements.md` J-양보

---

## 4. 경계가 깨질 때 — 관측된 사례

**증상에서 모델로 역추적하는 표다.** 새 증상을 만나면 여기 추가한다.

| 증상 | 깨진 경계 | 왜 |
|---|---|---|
| `planned = 0` · `chain-blocked: 모듈 몸통` | ③ — 포트가 벽을 안 마주 본다 | 포트가 기둥 끝(N/S)인데 계단꼴이 면 진입을 그린다 (2026-08-18) |
| 포트 상자가 면 속에 갇힌다 | ①↔③ 접점 — `anchor` 가 경계가 아니다 | 같은 면의 깊은 줄이 얕은 상자를 감싼다 |
| `reservation not emittable` (반출) | ④ — 예약 불변식 파손 | 남이 그 칸을 먼저 밟았다. 대개 ③의 탐색 폴백 |
| `예약 무시 재시도 — 남의 계획을 밟는다` | ③↔④ — **예약 주체가 둘** | 납품과 반출이 서로의 장부를 모른다 |
| 벨트가 띄엄띄엄 | ① — 벨트 연속성 | 좌석 행 *목록*을 돌고 *범위*를 안 돌았다 (2026-08-17) |
| `chain-blocked: 반출 예약` | §3.5 #8·#15 — C 가 협상 없이 박았다 | **직진** 반출은 ③의 공동 배정에 안 들어간다 |
| 다른 두 유체가 한 통이 된다 | §3.5 #7·#13 — **인접** 미검사 | `blocked`·`occ` 는 겹침만 본다. 파이프는 닿기만 해도 이어진다 |

---

## 5. 새 능력을 넣기 전에 던질 질문

```
1. 이건 **어느 모델의 일인가**  — 넷 중 하나에 안 들어가면 새 모델이다(비싸다)
   행 채널(⑤)의 일이면 **모델부터 세워야 한다** — 이름만 있고 계획이 없다
0. 그 앞에: **도형 문제인가 자리 문제인가** (§3.5 · §4). 자리 문제면 새 도형을 만들어도
   안 풀린다 — 2026-08-18 에 도형을 세 번 바꾸고서야 자리 문제인 걸 알았다
0b. 자리 문제면: **그 주체가 절대 좌표로 쓰나** (§3.5 전수표). 그렇다면 새 능력이 아니라
   **양보**나 **로컬 좌표**가 답이다 — ③이 경쟁을 푸는 이유가 그것이다
2. 그 모델이 **그릴 수 있는 도형인가** — 아니면 도형을 먼저 늘려야 한다
3. 접점의 뜻이 바뀌나 — `ModulePort.anchor` 같은 것.  바뀌면 **양쪽을 같이** 고친다
```

**2번을 건너뛰면 게이트를 여는 것으로 착각한다.** `eligible` 을 넓혔던 것이 그 예다 —
조건을 통과시켰지만 그릴 수 있는 도형은 안 늘어서, 못 그리는 그림이 나왔다.
