---
tags: [auto-layout, placement, routing, fluid]
aliases: [트렁크 파이프, trunk-pipe, 유체 레시피]
---

> **부모 문서:** [auto-layout-wizard.md](auto-layout-wizard.md)
> **앞선 결정:** [.trunk-redesign §10](auto-layout-wizard.trunk-redesign.md) — 아이템 쪽 트렁크(탭 인서팅)
> **용어:** [트렁크 파이프](용어사전.md#트렁크-파이프-trunk-pipe) (2026-07-13, 사용자 명명)

# 트렁크 파이프 — 유체를 모듈 파이프라인에 들이기

## 0. 한 줄 요약

파이프 **한 줄**이 기둥을 따라 달리며 머신들의 유체 입구를 **죽 이어서** 공급한다.
아이템의 [탭 인서팅](용어사전.md#탭-인서팅-tap-inserting)과 같은 자리에 서는 유체판이다.

## 1. 그림 먼저 — 무엇이 다른가

아이템은 이렇게 생겼다. 벨트가 면을 따라 흐르고, 머신마다 **인서터**가 하나씩 붙어 집어간다.

```
   x=-2 -1  0  1  2
 y= 0    B   i  M  M  M      B = 트렁크 벨트   i = 탭 인서터
 y= 1    B   .  M  M  M      M = 머신
 y= 2    B   .  M  M  M
 y= 3    B   i  M  M  M
 y= 4    B   .  M  M  M
 y= 5    B   .  M  M  M
```

유체는 이렇게 생겼다. **인서터가 없다.** 파이프가 머신의 **유체 입구 칸에 직접 닿아야** 한다.

```
   x=-1  0  1  2
 y= 0    P  M  M  M         P = 트렁크 파이프  (머신에 바로 붙는다)
 y= 1    P  M  M  M
 y= 2    P  M  M  M
 y= 3    P  M  M  M
 y= 4    P  M  M  M
 y= 5    P  M  M  M
```

세 가지가 다르다:

1. **거리가 강제된다.** 인서터는 팔이 있어서 벨트를 1~2칸 떨어뜨려 놓을 수 있다. 파이프는 팔이 없다 —
   머신 면에 **딱 붙어야**(바깥 1칸) 한다.
2. **칸이 강제된다.** 벨트는 머신 둘레 아무 칸에나 인서터를 앉힐 수 있다. 유체는 **프로토타입이 정한
   칸**(`fluid_boxes`)에만 꽂을 수 있다. 화학 공장이라면 정해진 두 칸뿐이다.
3. **합류가 공짜다.** 벨트는 한 줄에 여러 품목을 못 실어서 트렁크라는 장치가 필요했다. 파이프는 같은
   유체면 그냥 이어 붙이면 하나의 네트워크가 된다 — 트렁크 파이프는 "합치는 기술"이 아니라
   **그냥 자연스러운 모양**이다.

## 2. 충돌 — 유체 입구가 이웃 머신에 막힌다

우리 모듈은 머신을 **세로로 밀착해** 쌓는다(`MODULE_ROW_GAP = 0`). 노출된 긴 면은 W 와 E 이고,
벨트 트렁크가 거기를 훑는다.

그런데 화학 공장의 유체 입구는 **N 면**(위)에, 출구는 **S 면**(아래)에 박혀 있다. 기둥에서 중간 머신의
N 면은 **바로 위 머신의 S 면**이다. 밀착해 있으니 **유체 입구를 꽂을 칸이 아예 없다.**

```
  ▒▒▒   머신 0
  ▒▒▒
  ▒▒▒        ← 머신 0 의 S = 머신 1 의 N. 유체 입구가 서로 맞물려 봉인됐다.
  ▒▒▒   머신 1
  ▒▒▒
  ▒▒▒
```

**"면 = 역할"(출력→W, 자식-공급 입력→E)이라는 우리 규칙과 머신 프로토타입이 정면으로 부딪힌다.**

## 3. 결정 — 기둥은 그대로, 머신을 돌린다 (2026-07-13, 사용자)

세 가지 길이 있었다:

| 안 | 내용 | 왜 안 골랐나 / 골랐나 |
|---|---|---|
| 가로 행 | 유체 노드만 머신을 옆으로 나란히 | 아이템 벨트가 N/S 로 밀리고, 모듈마다 방향이 달라져 패킹·채널이 그걸 감당해야 함 |
| **세로 기둥 + 머신 90° 회전** | 기둥은 유지하고 **머신만** 돌려 유체 입구가 W/E 를 보게 | **채택.** 아래 참고 |
| 기둥 + 머신 간격 벌리기 | gap 을 되살려 N/S 입구를 노출 | 파이프가 머신마다 갈라져 내려와야 함 — **트렁크 파이프가 아니게 된다** |

### 왜 회전이 싼가 — 전제를 하나도 안 건드린다

화학 공장은 **3×3 정사각형**이다. 돌려도 footprint 가 그대로다. 패킹·채널 기하·홉·반출이 전부
"세로 기둥 + W/E 면" 위에 서 있는데, **그 전제가 하나도 안 깨진다.** 바뀌는 건 유체 상자가 어느 면을
보느냐, 그것 하나다.

> 정사각형이 아닌 머신(2×3 등)이 유체 레시피에 오면 이 전제가 깨진다. v1 은 그 경우 폴백한다.

### 왜 회전 방향이 저절로 맞는가

Factorio 는 유체 **입력과 출력을 반대 면**에 둔다(화학 공장: 입력 N, 출력 S). 시계방향 90° 를 주면
N→E, S→W 가 된다. 즉 **유체 입력이 입력 면(E)에, 유체 출력이 출력 면(W)에** 온다 —
우리가 아이템에 쓰던 "출력→W, 입력→E" 와 **같은 방향**이다. 우연이 아니라 게임 쪽 설계가 우리와
같은 이유로 그렇게 되어 있는 것이다.

**단, 각도를 상수로 박지 않는다.** 프로토타입마다 유체 상자 위치가 다르므로 "화학 공장은 CW" 를
박으면 다른 머신에서 깨진다. **네 방향을 다 시험해 유체 입력 상자가 입력 면(E)을 보는 회전을
고른다** — 데이터가 결정하고, 결정적이다.

## 4. depth 규약 — 파이프가 1칸을 먹으면 아이템이 밀린다

머신 면에서 바깥으로 센 칸 수를 `depth` 라 한다(0 = 머신 자기 가장자리).

| 무엇 | depth | 이유 |
|---|---|---|
| 트렁크 파이프 | **1** | 머신 유체 입구에 닿아야 한다. 선택의 여지가 없다. |
| 아이템 벨트 — 가까운 레인 | 2 | 일반 인서터가 1칸에 앉아 2칸에서 집는다 |
| 아이템 벨트 — 먼 레인 | 3 | 긴팔 인서터가 1칸에 앉아 3칸에서 집는다 |
| **아이템 벨트 — 파이프 넘김(케이스 B)** | **4** | 1칸을 파이프가 먹었다. 긴팔이 **2칸**에 앉아 **4칸**에서 집고 머신 가장자리(0칸)에 떨군다 |

**케이스 B 의 결과: 파이프가 지나가는 면은 아이템 레인을 하나밖에 못 준다**(긴팔 전용, depth 4).
일반 인서터는 1칸에 앉아야 하는데 그 자리가 파이프이므로 아예 못 쓴다.

```
   x=-4 -3 -2 -1  0  1  2
 y= 0    B  .  i  P  M  M  M      P = 트렁크 파이프 (d=1)
 y= 1    B  .  .  P  M  M  M      i = 긴팔 인서터   (d=2)
 y= 2    B  .  .  P  M  M  M      B = 아이템 벨트   (d=4)
```

즉 유체 입력 하나짜리 화학 공장의 면 용량은:

- **E 면**(유체가 사는 곳): 파이프 1줄 + 아이템 1줄(긴팔, d=4)
- **W 면**: 아이템 2줄(일반 d=2, 긴팔 d=3)

`plastic-bar`(petroleum-gas 유체 + coal 아이템 → plastic-bar 아이템)는 유체 1 + 아이템 입력 1 +
아이템 출력 1 = **정확히 들어간다.**

## 5. v1 범위 — 유체 **입력** 하나뿐 (2026-07-13, 사용자)

받는 것:

- 노드의 **유체 입력**이고, 그 유체를 트리 안의 자식이 만들지 **않는** 것(= 외부 공급).
  외부 공급이므로 무한파이프에서 들어온다.

거절하고 옛 경로로 폴백하는 것:

- 자식이 유체를 만들어 부모에 넘기는 경우(**유체 홉**) — `moduleHop` 이 벨트만 안다.
- 루트가 유체를 뱉는 경우(**유체 출력 반출**).
- 유체 줄이 2개 이상인 노드.
- 정사각형이 아닌 머신.

**이 범위를 고른 이득: `moduleHop` 을 안 건드린다.** 유체가 외부 공급이면 홉이 아니라 반출만 필요하다.

## 6. 유체 상자를 어느 것으로 고르나

머신에 유체 입력 상자가 여러 개일 수 있다(화학 공장은 2개). 줄은 하나다. 규칙:

1. `fb.filter` 가 그 유체 이름과 일치하는 상자가 있으면 그것.
2. 없으면 **우리가 고른 면 위에 있는 입력 상자 중 첫 번째**(프로토타입 등장 순서).

결정적이다. 같은 입력이면 같은 상자를 고른다.

## 7. 손대는 곳 (데이터 흐름 순서)

| 파일 | 지금 | 바뀔 것 |
|---|---|---|
| `containerModel.Container` | 방향 필드 없음 | `direction?` 추가 |
| [`machinePlacer.commitContainer`](../frontend/src/utils/autoLayout/machinePlacer.ts) | `direction: 0` **하드코딩** | 컨테이너 방향을 셀로 내려보냄 |
| [`moduleWizard`](../frontend/src/utils/autoLayout/planner/moduleWizard.ts) | 유체 하나면 **트리 전체** 폴백 | §5 범위만 통과 · `kind: "pipe"` 로 줄 생성 |
| [`clusterPortPlanner`](../frontend/src/utils/autoLayout/module/clusterPortPlanner.ts) | pipe 보면 즉시 `complex` | 파이프 슬롯(면 = 유체 상자가 정함, depth 1, 인서터 없음) + 케이스 B |
| [`clusterModule`](../frontend/src/utils/autoLayout/module/clusterModule.ts) | pipe 줄 버림 | **트렁크 파이프 방출기** + 머신 회전 각도 결정 |
| [`modulePerimeterPass`](../frontend/src/utils/autoLayout/planner/modulePerimeterPass.ts) | 반출 = 벨트 + 인서터 | 유체 포트는 **파이프**로 반출, 끝에 `infinity-pipe` |
| `moduleHop` | — | **안 건드린다**(§5) |

## 8. 아직 안 하는 것

- **지하 파이프**(`pipe-to-ground`) — 파이프끼리의 교차. 아이템에서 지하벨트가 필요했던 것과 같은
  문제가 유체에도 온다. 유체 홉이 생기면 그때.
- **유체 홉** — 자식→부모 유체 전달.
- **처리량** — 파이프 한 줄의 유체 처리량 한계(길이에 따라 떨어진다). 펌프가 필요해지는 지점.
  [determineBeltCount](용어사전.md#determinebeltcount) 의 유체판이 될 자리.
